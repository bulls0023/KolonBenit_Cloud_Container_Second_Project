# 부트스트랩 검증 전용 프로파일
#
# 목적: 컨텍스트 기동 + SecurityFilterChain 구성 + @ConfigurationProperties
#       바인딩을 검증한다. WAS 없이 돈다.
app:
  jwt:
    signing-key: test-signing-key-must-be-at-least-32-bytes-long!!
  cookie:
    secure: false
  was:
    base-url: http://localhost:19999   # 호출하지 않는다
