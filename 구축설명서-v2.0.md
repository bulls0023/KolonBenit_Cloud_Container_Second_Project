# Hybrid Toy Project — 구축설명서 v2.0

> **상위 문서: `README.md` v4.0.** 충돌 시 README가 이긴다.
> 이 문서는 **"누가 무엇을 어떤 순서로 만들고 검증했는가"** 만 다룬다.
> 설계 근거·계약 스펙은 README에 있다. **여기서 반복하지 않는다.**

| 항목 | 값 |
|---|---|
| 문서 버전 | **v2.0 (최종)** |
| 기준일 | 2026-08-13 |
| 상태 | **G1 → G3 완료 + 관측성(S7) 완료.** OKD Web 애플리케이션은 범위 밖 이관 |
| 대상 | 인프라 리드 / WAS / BFF / Web / DB / OKD |
| 상위 문서 | `README.md` v4.0 |

---

## 이 문서의 범위

| | **README v4.0** | **구축설명서 v2.0 (이 문서)** |
|---|---|---|
| 다루는 것 | 무엇을 약속했는가 · 왜 그렇게 설계했는가 · 무엇이 금지인가 | 누가 무엇을 어떤 순서로 만들었는가 |
| 내용 | 아키텍처, 계약, 표준, 설계 판단, 보안 기준, 트러블슈팅, 결정 로그 | **산출물 트리, 실행 절차, 검증 명령, 체크리스트, 게이트 기록, 배포 이력, 날짜, 개정 이력** |
| 성격 | 시간에 독립적 | **시간에 종속적** |

> ⚠️ **README v4.0에서 절 번호가 전면 재편되었다.** 이 문서의 모든 `README §x` 참조는 **v4.0 기준**이다.
> 구 번호(v3.2)를 쓰는 코드 주석이 남아 있다면 README 부록 A 매핑표를 본다.

---

## 목차

- [§0. 이 문서를 읽는 순서](#0-이-문서를-읽는-순서)
- [§1. 산출물 전체 목록](#1-산출물-전체-목록)
- [§2. 역할 분담 — 생성 가능 / 사람이 해야 하는 것](#2-역할-분담--생성-가능--사람이-해야-하는-것)
- [§3. PowerShell 표준 절차](#3-powershell-표준-절차)
- [§4. 실행 순서 총괄](#4-실행-순서-총괄)
- [§5. S0 — 착수 전 리드 선행 작업](#5-s0--착수-전-리드-선행-작업)
- [§6. S1 — DB팀](#6-s1--db팀)
- [§7. S2 — WAS 담당](#7-s2--was-담당)
- [§8. S3 — BFF 담당](#8-s3--bff-담당)
- [§9. S4 — Web 담당](#9-s4--web-담당)
- [§10. S5 — EKS 배포 → G2](#10-s5--eks-배포--g2)
- [§11. S6 — 내부 흐름 → G3](#11-s6--내부-흐름--g3)
- [§12. S7 — 관측성 구축](#12-s7--관측성-구축)
- [§13. 통합 검증 시나리오](#13-통합-검증-시나리오)
- [§14. 배포 전 체크리스트](#14-배포-전-체크리스트)
- [§15. 완료 기준(DoD) 체크리스트](#15-완료-기준dod-체크리스트)
- [§16. 장애 시 롤백](#16-장애-시-롤백)
- [§17. 코드 산출 이력 및 배치 원칙](#17-코드-산출-이력-및-배치-원칙)
- [§18. 배포 이력](#18-배포-이력)
- [§19. 프로젝트 종료 절차](#19-프로젝트-종료-절차)
- [§20. 문서 개정 이력](#20-문서-개정-이력)
- [부록 A. 게이트 통과 기록](#부록-a-게이트-통과-기록)

---

## §0. 이 문서를 읽는 순서

| 역할 | 필독 |
|---|---|
| 인프라 리드 | 전체 |
| DB 담당 (`kusdb`) | §1, §2, §3, §5, §6, §13 |
| WAS 담당 (`kuswas`) | §1, §2, §3, §7, §13 |
| BFF 담당 (`kusbff`) | §1, §2, §3, §8, §11, §13 |
| Web 담당 (`kusweb`) | §1, §2, §3, §9, §10, §13 |
| OKD 담당 | §2, §11 + README §14 |

**전 팀 공통 3원칙**

1. **계약이 코드보다 위다.** README §6과 다르게 구현했으면 코드가 틀린 것이다.
2. **각 단계는 게이트를 통과해야 다음으로 간다.** 검증 명령이 없는 "다 됐어요"는 완료가 아니다.
3. **식별자는 손으로 옮기지 않는다.** ARN·도메인·토큰은 `output -raw` → 변수 → 명령 인자 (README §12.2).

---

## §1. 산출물 전체 목록

```
repo/
├── README.md                                   [리드]    v4.0 — 기술 가이드라인
├── 구축설명서-v2.0.md                           [리드]    이 문서
│
├── infra/
│   ├── backend-bootstrap/                      [리드]    S3 state 버킷 (최초 1회)
│   ├── envs/dev/                               [리드]    유일한 apply 지점
│   └── modules/{network,eks,ecr,rds}/          [각 팀]
│
├── db/                                          [DB팀]
│   ├── 01_schema.sql                                     계정·업무·진료·처방 테이블 DDL
│   ├── 02_grants.sql                                     app_was 최소 권한 + REQUIRE SSL
│   ├── 03_seed.sql                                       직원·의사·진료슬롯 seed
│   ├── 04_icd_seed.sql                                   KCD 상병코드 (14,283 + 37,543행)
│   ├── raw/data.json                                     공공데이터 원본 19MB / 47,798행
│   └── tools/
│       ├── gen-bcrypt.ps1                                BCrypt 해시 생성기
│       └── convert-icd.ps1                               data.json → 04_icd_seed.sql
│
├── apps/was/                                    [WAS]     Spring Boot 4.1.0
│   ├── Dockerfile / .dockerignore                        멀티스테이지 / non-root / amd64
│   ├── build.gradle.kts, settings.gradle.kts, gradle/wrapper/
│   └── src/main/
│       ├── resources/application.yaml                    SNAKE_CASE / sslMode / TZ
│       └── java/.../
│           ├── config/           SecurityConfig(내부용), PasswordEncoderConfig, JacksonConfig
│           ├── web/filter/       ActorHeaderFilter(2차 검증), TraceIdFilter
│           ├── auth/             PatientAuthController/Service, StaffAuthController/Service
│           │                     LoginAttemptService (REQUIRES_NEW)
│           ├── domain/           PatientUser, StaffUser, Doctor, Appointment,
│           │                     Chart, Prescription, PrescriptionItem,
│           │                     IcdCode, IcdCodeSynonym
│           ├── repository/       JPA Repository
│           ├── biz/              PatientApiController, StaffApiController,
│           │                     ChartService(차트+처방 단일 트랜잭션)
│           ├── error/            ApiError, GlobalExceptionHandler, ErrorCode
│           └── health/           HealthController(/healthz, /readyz-DB)
│
├── apps/bff/                                    [BFF]     Spring Boot 4.1.0
│   ├── Dockerfile / .dockerignore
│   ├── build.gradle.kts, settings.gradle.kts, gradle/wrapper/
│   └── src/main/
│       ├── resources/application.yaml
│       └── java/.../
│           ├── config/           SecurityConfig, JwtConfig(Nimbus), RestClientConfig
│           ├── security/         TokenResolver, ActorBindingValidator, JwtIssuer
│           ├── filter/           TraceIdFilter, InboundActorHeaderStripFilter
│           ├── patient/          PatientAuthController, PatientApiController
│           ├── staff/            StaffAuthController, StaffApiController
│           ├── client/           WasClient (JdkClientHttpRequestFactory, 2s/5s, retry 없음)
│           ├── error/            ApiError, GlobalExceptionHandler
│           └── health/           HealthController(/healthz, /readyz)
│
├── apps/patient-web/                            [Web]
│   ├── Dockerfile                                        nginx-unprivileged:1.27-alpine
│   ├── nginx.conf                                        8080 / SPA fallback / staff 404 / charset
│   └── (SPA 소스)
│
├── local/                                       [EKS팀]   클러스터 없이 전 구간 통합 검증
│   ├── docker-compose.yml                                web→bff→was→mysql
│   ├── prepare.ps1                                       digest·시크릿·mysql-init 생성
│   ├── mysql-init/                                       01~04 sql 참조
│   ├── verify-was.ps1                                    WAS 계약 38개
│   └── verify-bff.ps1                                    BFF 계약 45개
│
├── k8s/                                         [EKS팀]
│   ├── namespace/                                        Namespace + LimitRange + ResourceQuota
│   ├── was/{configmap,secret.example,deployment,service}.yaml
│   ├── bff/{configmap,secret.example,deployment,service}.yaml
│   ├── patient-web/{configmap,deployment,service}.yaml
│   ├── cloudflared/{secret.example,deployment}.yaml
│   └── ingress/patient-web-ingress.yaml                  2-path 확정본
│
├── monitoring/                                  [리드]    S7 관측성
│   ├── cw-trust.json                                     Pod Identity 신뢰 정책
│   └── cw-addon-config.json                              애드온 구성값
│
└── scripts/
    ├── build-push.ps1                           [EKS팀]  git SHA 태그 빌드·푸시
    ├── pin-base-images.ps1                      [EKS팀]  베이스 이미지 digest 조회
    ├── preflight.ps1                            [EKS팀]  배포 전 점검 17개
    ├── deploy.ps1                               [EKS팀]  빌드·푸시·apply 일괄
    ├── verify-flow1.ps1                         [리드]   G2 검증 24개
    └── verify-flow2.ps1                         [리드]   G3 검증 29개
```

---

## §2. 역할 분담 — 생성 가능 / 사람이 해야 하는 것

### 2.1 계약에서 기계적으로 유도된 산출물

| # | 산출물 | 근거 계약 |
|---|---|---|
| 1 | `db/01_schema.sql` | README §6.8 |
| 2 | `db/02_grants.sql` | README §9.2 |
| 3 | `db/03_seed.sql` | README §6.8 |
| 4 | `db/tools/gen-bcrypt.ps1` | — (**비밀번호 값은 사람이 입력**) |
| 5 | `db/tools/convert-icd.ps1` | README §6.10 |
| 6 | `db/04_icd_seed.sql` | 5번의 산출물 — 14,283 + 37,543행 |
| 7 | `apps/was/**` 전체 | README §6.5~§6.8 |
| 8 | `apps/bff/**` 전체 | README §6.5~§6.7 |
| 9 | `apps/*/Dockerfile` | README §5.3, §6.1 |
| 10 | `apps/patient-web/nginx.conf` + Dockerfile | README §6.7, §6.9 |
| 11 | `local/**` (compose + prepare + verify) | §13 |
| 12 | `k8s/**` 매니페스트 전량 | README §6.1~§6.4, §6.9 |
| 13 | `scripts/*.ps1` | §13, §14 |
| 14 | `monitoring/*.json` | README §10.1 |

### 2.2 생성 불가 — 구조적 이유

| 산출물 | 왜 불가 |
|---|---|
| `terraform.tfvars` 실값 | 커밋 금지 대상. 리드 로컬에만 존재 |
| 실제 Secret 매니페스트 | 값 자체가 시크릿 |
| 베이스 이미지 digest | 조회 시점에만 확정. **명령은 제공, 값은 사람이 채움** |
| Cloudflare 대시보드 설정 | UI 조작. API 토큰 미보유 |
| OKD Web 애플리케이션 | OKD팀 소관 |
| 실제 빌드·푸시·apply 실행 | 로컬 Docker·AWS 자격증명 필요 |
| 공공데이터포털 서비스키 | **불필요하다.** 오프라인 seed 채택 (README §7.11) |
| `data.json` 원본 다운로드 | 포털 로그인·약관 동의 필요. **파일은 이미 확보되어 `db/raw/` 에 커밋됨** |

### 2.3 시크릿 — 절대 위임 불가

| # | 항목 | 주체 | 생성 방법 | 전달 대상 |
|---|---|---|---|---|
| 1 | `JWT_SIGNING_KEY` | 리드 | 아래 명령 | BFF 담당 |
| 2 | RDS 마스터 비밀번호 조회 | 리드/DB | Secrets Manager | DB팀 (계정 생성 전용) |
| 3 | `app_was` 비밀번호 | DB팀 | 임의 강력 문자열 | WAS 담당 |
| 4 | 직원 seed 평문 비밀번호 | DB팀 | 팀 내부 합의 | 테스트용, 문서화 금지 |
| 5 | Cloudflare Tunnel Token | 리드 | 대시보드 | BFF 담당 |
| 6 | Service Token ID / Secret | 리드 | 대시보드 (**1회만 표시**) | OKD팀 |

```powershell
# JWT_SIGNING_KEY — 48바이트 랜덤 Base64
$b = New-Object byte[] 48
[System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b)
[Convert]::ToBase64String($b)
```

```powershell
# RDS 마스터 자격 (계정 생성에만 사용)
aws secretsmanager get-secret-value --secret-id "<rds_master_secret_arn>" --query SecretString --output text
```

> 🚨 위 6개는 **채팅·이슈·PR·README·스크린샷·터미널 히스토리 어디에도 남기지 않는다.**
> 🚨 관측성 도입 이후 애플리케이션 로그는 CloudWatch Logs에 영속 저장된다 (README §10.3). 로그 노출은 즉시 외부화된다.

**⚠️ 실사고 기록** — nginx 설정 진단 중 Cloudflare Service Token의 Client ID·Secret이 평문으로 노출된 사례가 있었다. 노출이 확인되면 **즉시 폐기·재발급**한다. Service Token은 재발급이 유일한 대응이다.

### 2.4 Cloudflare 대시보드 — 전량 수동

| # | 작업 | 위치 | 실패 시 증상 |
|---|---|---|---|
| 1 | SSL/TLS = **Full (strict)** | SSL/TLS → Overview | 무한 리다이렉트 루프 |
| 2 | ACM 검증 CNAME (**⚪ 회색**) | DNS | 🟠 주황이면 인증서 영구 Pending |
| 3 | `www` CNAME → ALB DNS (**🟠 주황**) | DNS | ⚪ 회색이면 WAF·DDoS 무력화 |
| 4 | WAF Managed Ruleset 활성화 | Security → WAF | — |
| 5 | Rate Limiting Rule | Security | — |
| 6 | Bot Fight Mode | Security → Bots | — |
| 7 | Tunnel 생성 + Token 발급 | Zero Trust → Networks → Tunnels | — |
| 8 | Public Hostname 등록 | Tunnel 설정 | `staff-api` DNS 자동 생성 |
| 9 | Access Application 생성 | Zero Trust → Access | — |
| 10 | Policy **Action = `Service Auth`** | Access → Policies | `Allow`면 OKD가 HTML 수신 |
| 11 | **Protect with Access 활성화 + AUD 선택** | Tunnel 설정 | 미설정 시 Access 우회 가능 |
| 12 | Service Token 발급 | Zero Trust → Access → Service Auth | — |

> 🚨 **10번과 11번이 P3 최다 실패 지점이다.** 둘 다 확인하기 전에는 OKD팀에 착수 지시를 내리지 않는다.
> 🚨 **2번과 3번의 구름 색이 반대다.** 인증서 검증은 ⚪ 회색, 서비스 트래픽은 🟠 주황.
> 🚨 **`staff-api` DNS 레코드를 미리 만들지 않는다.** Tunnel이 자동 생성하며 수동 레코드가 있으면 충돌한다.

### 2.5 로컬 환경 · 실행 행위

| # | 작업 | 주체 |
|---|---|---|
| 1 | Docker Desktop 설치 및 기동 | EKS팀 3인 |
| 2 | JDK 25 설치 (Corretto 권장) | WAS·BFF |
| 3 | 베이스 이미지 **digest 조회 → 기록 → 커밋** | EKS팀 3인 |
| 4 | 이미지 빌드·푸시 (ECR 로그인 포함) | EKS팀 3인 |
| 5 | `kubectl apply` 실행 | 각 담당 |
| 6 | `kubectl create secret` 실행 | 각 담당 |
| 7 | 임시 파드로 RDS 접속 및 SQL 실행 | DB팀 |
| 8 | `terraform apply` | **리드 전용** |
| 9 | CloudWatch 애드온 설치 | **리드 전용** |

```powershell
# 베이스 이미지 digest 조회 — 결과를 README §5.4 표에 기록
foreach ($img in @("amazoncorretto:25-alpine","nginxinc/nginx-unprivileged:1.27-alpine","cloudflare/cloudflared:latest")) {
  docker pull $img | Out-Null
  docker inspect --format="{{index .RepoDigests 0}}" $img
}
```

### 2.6 판단이 필요했던 결정 — 전부 확정 완료

| # | 항목 | 주체 | 결과 |
|---|---|---|---|
| 1 | 직원 테스트 계정 수·역할 구성 | DB + OKD | 3역할 (DOCTOR / NURSE / ADMIN_STAFF) |
| 2 | 진료 슬롯 시간 단위 | DB + WAS | 확정 |
| 3 | ICD 코드 seed 범위 | 리드 | A안 — README §15-28 |
| 4 | Rate Limit 임계값 | 리드 | 확정 |
| 5 | 데모 시나리오 (G2/G3 시연 순서) | 전 팀 | 확정 |
| 6 | 관측성 도입 범위 | 리드 | CloudWatch 단일 트랙 — README §15-45 |

---

## §3. PowerShell 표준 절차

> P2에서 **PowerShell 5.1 파일 입출력 문제로만 5회** 막혔다. 아래를 지키면 전부 예방된다.
> 원칙의 근거는 README §12.1.

**① 세션 시작 3줄** — 새 창을 열 때마다 필요하다

```powershell
[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding $false
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
Get-ChildItem -Recurse -Include *.ps1 | Unblock-File
```

zip으로 받은 `.ps1` 은 인터넷 표식(MOTW)이 붙어 있어 `Unblock-File` 이 필요하다.
매번 치기 싫으면 사용자 범위로 한 번만 설정한다 (관리자 권한 불필요).

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
Add-Content $PROFILE '[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding $false'
```

**② 파일 치환은 이 함수만 쓴다**

```powershell
function Set-Placeholder([string]$Path, [string]$From, [string]$To) {
    # .NET 은 PowerShell 의 현재 위치를 모른다. 절대 경로로 변환한다.
    $full = (Resolve-Path $Path).Path
    $enc  = New-Object System.Text.UTF8Encoding $false      # BOM 없음
    $text = [System.IO.File]::ReadAllText($full, [System.Text.Encoding]::UTF8)
    if ($text -notlike "*$From*") { Write-Host "  [SKIP] $Path" -ForegroundColor Yellow; return }
    [System.IO.File]::WriteAllText($full, $text.Replace($From, $To), $enc)
    Write-Host "  [OK] $Path" -ForegroundColor Green
}
```

⚠️ **`Set-Content -Encoding UTF8` 을 쓰지 않는다.** PS 5.1은 BOM을 붙이고 원본을 CP949로 읽는다.
BOM은 YAML의 첫 키를, Dockerfile의 `FROM` 을, SQL의 첫 구문을, **AWS CLI에 넘기는 JSON의 파싱을** 깨뜨린다.

**③ 치환·생성 후 반드시 확인한다**

```powershell
Format-Hex $path | Select-Object -First 1        # 첫 바이트가 EF BB BF 면 실패
[System.IO.File]::ReadAllText((Resolve-Path $path).Path, [System.Text.Encoding]::UTF8) | Select-String '검색어'
```

`Get-Content -Encoding UTF8` 도 5.1에서는 신뢰할 수 없다. 확인은 `ReadAllText` 로 한다.

**④ 생성하는 `.ps1` 은 UTF-8 BOM + CRLF 로 저장한다**

PS 5.1은 BOM 없는 `.ps1` 을 CP949로 파싱해 한글 주석에서 구문 오류를 낸다. **②·④의 BOM 정책이 서로 반대다** — 데이터 파일은 BOM 없음, 스크립트 파일은 BOM 있음.

**⑤ 값을 명령행에 직접 넣지 않는다**

```powershell
# 나쁨 — 히스토리에 남고, 따옴표가 벗겨질 수 있다
aws secretsmanager put-secret-value --secret-string '{"K":"value"}'

# 좋음 — 파일 경유 후 삭제
$tmp = Join-Path $env:TEMP 's.json'
[System.IO.File]::WriteAllText($tmp, ($obj | ConvertTo-Json -Compress), (New-Object System.Text.UTF8Encoding $false))
aws secretsmanager put-secret-value --secret-string "file://$tmp"
Remove-Item $tmp -Force
```

**⑥ `$ErrorActionPreference = 'Stop'` 을 네이티브 명령 스크립트에 걸지 않는다**

`aws`·`docker`·`kubectl` 은 정상 동작 중에도 stderr에 쓴다. `'Stop'` 이면 그것만으로 스크립트가 죽는다.
`'Continue'` 로 두고 판정을 **전부 `$LASTEXITCODE`** 로 한다.

**⑦ 명령을 여러 줄로 나누어 붙여넣지 않는다**

백틱 줄바꿈 + 붙여넣기 조합에서 ARN이 조용히 절단된 사고가 3회 있었다. 식별자가 들어가는 명령은 **변수에 담아 단일 행**으로 실행한다 (README §12.2).

**⑧ PS 5.1 한계를 만나면 `pwsh`(PS7)로 넘어간다**

`ConvertFrom-Json` 의 19MB 파싱 한계(`maxJsonLength`), `-Encoding utf8NoBOM` 미지원이 대표적이다.

---

## §4. 실행 순서 총괄

### 4.1 마일스톤

| 단계 | 내용 | 게이트 | 완료일 |
|---|---|---|---|
| **P0** | 설계 확정 · AWS 불필요 작업 (전 팀 병렬) | — | 2026-08-08 |
| **P1** | 인프라 리드 단독 apply | **G1** | 2026-08-10 |
| **S0** | 착수 전 리드 선행 작업 | S0 | 2026-08-10 |
| **S1** | DB팀 — 스키마·계정·seed | **D1 / D2** | 2026-08-12 |
| **S2** | WAS 구현·배포 | **W1 / D3 / W2** | 2026-08-12 |
| **S3** | BFF 구현·배포 | **B1 / B2** | 2026-08-12 |
| **S4** | Web 구현·Ingress | — | 2026-08-12 |
| **S5** | EKS 배포 + Cloudflare | **G2** | 2026-08-12 |
| **S6** | 내부 흐름 (Tunnel + Access) | **G3** | 2026-08-13 |
| **S7** | 관측성 (CloudWatch) | **M1** | 2026-08-13 |

### 4.2 의존 관계

```
                  ┌─→ ECR ─────────────────────────────┐
인프라 apply ─G1─┤                                      ├─→ 이미지 빌드/푸시 (전원 즉시 병렬)
                  └─→ EKS / RDS                         ┘
                        │
              DB 스키마 ─┴─→ WAS ─→ BFF ─→ Web ─→ Ingress ─G2─→ 흐름1 완료
                                      └─→ cloudflared ────G3─→ 흐름2 완료
                                                              └─→ CloudWatch ─M1─→ 관측성
```

**`DB → WAS → BFF → Web` 이 임계 경로다.** 이미지 빌드·푸시는 전원 즉시 병렬 가능하며 **배포 순서만 직렬이다.**

### 4.3 게이트 정의

| 게이트 | 조건 |
|---|---|
| **G1** | `terraform output` 전체 공유 + 전 팀원 `kubectl get nodes` 성공 |
| **S0** | 리드 선행 6항목 + 팀원 4명 환경 점검 통과 |
| **D1** | `app_was` 자격으로 `SELECT 1` + `Ssl_cipher` 비어 있지 않음 |
| **D2** | `icd_code` 14,283 / `icd_code_synonym` 37,543 + `app_was` 읽기 전용 |
| **W1** | WAS `gradlew build` — 컨텍스트 기동 + `@Query` JPQL 전량 파싱 |
| **D3** | WAS `/readyz` → `db:up` (= `ddl-auto: validate` 통과) |
| **W2** | `verify-was.ps1` **38/38** |
| **B1** | BFF `/readyz` → `{"status":"ok"}` (WAS 미의존) |
| **B2** | `verify-bff.ps1` **45/45** |
| **G2** | `verify-flow1.ps1` **24/24** + **브라우저 실동작 10항목** |
| **G3** | `verify-flow2.ps1` **29/29** |
| **M1** | CW 파드 5개 Running + `app` 파드 스펙 불변 + 로그 그룹 4종 보존 7일 |

> 🚨 **D2는 WAS를 막지 않는다.** WAS 담당은 D1(커넥션)만 통과하면 착수한다.
> D2는 `GET /staff/icd-codes` 통합 검증 시점까지만 완료되면 된다. 임계 경로는 변하지 않는다.

---

## §5. S0 — 착수 전 리드 선행 작업

> **소요 ~30분. 이게 끝나기 전에는 어느 팀도 배포를 시작하지 않는다.**

| # | 작업 | 검증 |
|---|---|---|
| 1 | README + 이 문서 merge, 전 팀 공유 | PR merged |
| 2 | 구 문서에 **"README v4.0으로 대체됨"** 표기 | — |
| 3 | `JWT_SIGNING_KEY` 발급 → BFF 담당 전달 | BFF가 수신 확인 |
| 4 | G1 공유 항목 재공지 (README §17) | — |
| 5 | 팀별 담당 디렉토리·브랜치 규칙 확인 | README §3.1 |
| 6 | 레퍼런스 코드 세트 배포 (`feature/app-baseline`) | — |

**전 팀 환경 점검 (각자 1회)**

```powershell
aws sts get-caller-identity                                              # 본인 계정 확인
aws eks update-kubeconfig --region ap-northeast-2 --name hybrid-toy-eks
kubectl get nodes                                                        # Ready 2개
kubectl -n app get all                                                   # app 네임스페이스 접근 확인
docker version                                                           # Docker 기동 확인
```

**팀원 온보딩 안내문** (신규 PC 세팅 시)

```
1. winget install -e --id Amazon.AWSCLI
   winget install -e --id Kubernetes.kubectl
   → 설치 후 PowerShell 새 창을 열 것
2. aws configure          (AccessKey / SecretKey / ap-northeast-2 / json)
3. aws sts get-caller-identity                                            ← 본인 계정명 확인
4. aws eks update-kubeconfig --region ap-northeast-2 --name hybrid-toy-eks
5. kubectl get nodes                                                      ← Ready 2개

※ 4번을 건너뛰면 localhost:8080 오류. 클러스터 장애 아님.
※ PC를 옮기면 4번을 매번 다시 해야 한다.
※ 명령을 여러 줄로 나누지 말 것.
```

**🔒 게이트 S0**: 6개 항목 완료 + 팀원 4명 전원 환경 점검 통과.

---

## §6. S1 — DB팀

> **임계 경로의 시작점.** WAS가 이것을 기다린다. 소요 1~2시간.
> 계약은 README §6.8 / §9. 여기서는 실행만 다룬다.

| # | 작업 | 명령 / 검증 |
|---|---|---|
| 1 | 직원 계정 BCrypt 해시 생성 | `./db/tools/gen-bcrypt.ps1` → `$2b$` 로 시작 확인 |
| 2 | `01_schema.sql` 로컬 MySQL 8.0에서 선검증 | 에러 0 |
| 3 | `03_seed.sql` 에 해시 반영 (**평문 금지**) | 평문 없음 확인 |
| 4 | **git 커밋** — DDL/GRANT/SEED 3종 | `git ls-files db/` |
| 5 | 임시 파드로 RDS 접속 | 아래 |
| 6 | `commondb` 생성 → `01_schema.sql` 실행 | `SHOW TABLES;` |
| 7 | `02_grants.sql` 실행 | `SHOW GRANTS FOR 'app_was'@'%';` |
| 8 | `03_seed.sql` 실행 | `SELECT login_id, role, status, must_change_password FROM staff_user;` |
| 9 | `app_was` 계정으로 재접속 검증 | 아래 |
| 10 | 자격증명 → WAS 담당 전달 | **🔒 D1 — 여기서 WAS 담당이 착수한다** |
| 11 | `convert-icd.ps1` 실행 → `04_icd_seed.sql` 생성 | §6.1 |
| 12 | `04_icd_seed.sql` **git 커밋** | `git ls-files db/` |
| 13 | `04_icd_seed.sql` 실행 → 건수 검증 | **🔒 D2** |

> ⚠️ **4번·12번의 git 커밋이 유일한 데이터 복구 경로다.** `skip_final_snapshot = true` 이므로 RDS destroy = 데이터 완전 소실 (README §7.7).

**5번 — 임시 파드 접속**

```powershell
kubectl -n app run mysql-cli --rm -it --image=mysql:8.0 --restart=Never -- mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u admin -p
```

**9번 — `app_was` 로 TLS 접속 검증** (이게 통과해야 WAS가 뜬다)

```powershell
kubectl -n app run mysql-cli --rm -it --image=mysql:8.0 --restart=Never -- mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u app_was -p -e "SELECT 1; SHOW STATUS LIKE 'Ssl_cipher';"
```

> `Ssl_cipher` 가 **비어 있지 않아야** 한다. 비어 있으면 TLS 미적용 상태다.

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| `Access denied` | `REQUIRE SSL` 계정으로 평문 접속 시도 |
| 파드가 `Pending` | 노드 리소스. `kubectl -n app describe pod mysql-cli` |
| 접속 timeout | 파드가 `app` 네임스페이스가 아님 / RDS SG |
| `CREATE USER` 권한 오류 | `admin` 이 아닌 계정으로 실행 |
| `ERROR 1147 ... no such grant` | DB 단위 GRANT 후 테이블 단위 REVOKE 시도 (README §9.2) |

**🔒 게이트 D1** — 2026-08-12 통과.

### 6.1 ICD 상병코드 적재

> **D1 이후 작업이다. WAS를 막지 않는다.** 계약은 README §6.10.

**11번 — SQL 생성** (로컬 PowerShell, RDS 접속 불필요)

> ✅ **`04_icd_seed.sql` 은 이미 생성·검증되어 저장소에 있다. 이 단계는 건너뛰어도 된다.**
> KCD 개정본으로 다시 만들 때만 실행한다.

```powershell
cd db
.\tools\convert-icd.ps1 -InputPath .\raw\data.json -OutputPath .\04_icd_seed.sql
```

> ⚠️ PS 5.1은 19MB JSON 파싱에 수십 초가 걸리고 `maxJsonLength` 제한에 걸린다. 스크립트에 폴백이 있으나 **`pwsh`(PS7) 사용을 권한다.**

기대 출력:

```
[READ ] 47798 rows
[FILTER] excluded 10255  (완전코드구분='N' or 주상병사용구분='N')
[MASTER] icd_code          14283
[SYNONYM] icd_code_synonym 37543
[WRITE] 04_icd_seed.sql  (UTF-8 no BOM)
```

> ⚠️ **`[MASTER] 0` 이 나오면 필터가 반전된 것이다.** 원본에 `'Y'` 값은 존재하지 않는다. `<> 'N'` 이 맞다.

**13번 — 적재 및 검증**

파일이 크므로 대화형 셸에 붙여넣지 않는다.

```powershell
kubectl -n app run mysql-cli --image=mysql:8.0 --restart=Never --command -- sleep 3600
kubectl -n app cp .\db\04_icd_seed.sql mysql-cli:/tmp/04_icd_seed.sql
kubectl -n app exec -it mysql-cli -- bash -lc "mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u admin -p commondb < /tmp/04_icd_seed.sql"
kubectl -n app delete pod mysql-cli
```

**검증 SQL** — 한 번에 실행

```sql
SELECT (SELECT COUNT(*) FROM icd_code)                            AS icd_code,   -- 14283
       (SELECT COUNT(*) FROM icd_code_synonym)                    AS synonym,    -- 37543
       (SELECT COUNT(*) FROM icd_code_synonym WHERE code='E1140') AS e1140,      -- 60
       (SELECT COUNT(*) FROM icd_code_synonym s
          LEFT JOIN icd_code m ON m.code = s.code
         WHERE m.code IS NULL)                                    AS orphan;     -- 0

SELECT name_kr FROM icd_code WHERE code = 'E1140';
-- 기대: 당뇨병성 단일신경병증을 동반한 2형 당뇨병   (한글이 ??? 이면 인코딩 문제)

SELECT table_name, table_rows FROM information_schema.tables
 WHERE table_schema='commondb' AND table_name LIKE 'icd%';
-- 기대: 14000~14500 / 37000~38000 근사치.
-- 3 이나 2 가 나오면 ANALYZE TABLE 이 실행되지 않은 것이다.

SHOW GRANTS FOR 'app_was'@'%';                                    -- icd_* 는 SELECT 만
```

**실측 소요시간** — 5.8MB seed, MySQL 8.0.46 기준 **약 2초**. 수 분이 걸리면 배치 크기를 확인한다.

**⚠️ 흔한 실패**

| 증상 | 원인 | 조치 |
|---|---|---|
| 적재 0건 | 필터를 `= 'Y'` 로 작성 | `<> 'N'` 으로 교정 후 재생성 |
| `Duplicate entry 'E1140' for key 'PRIMARY'` | 마스터에 37,543행 전량 INSERT | 마스터/동의어 분리 |
| 한글이 `???` | seed 파일 BOM 또는 `SET NAMES` 누락 | UTF-8(BOM 없음) 재생성 |
| 실행이 수 분째 진행 | 단건 INSERT 37,543회 | 1,000행 배치로 재생성 |
| `Data too long for column 'name_en'` | `VARCHAR(255)` 미만 정의 | 원본 최대 223자. DDL 확인 |
| `ERROR 1451` (재적재 시) | `DELETE FROM icd_code` — 차트가 참조 중 | UPSERT 방식 seed 사용 |
| 적재 성공했는데 검색이 느림 | `ANALYZE TABLE` 누락 | seed 말미 `ANALYZE TABLE` 확인 |
| `ConvertFrom-Json` 실패 | PS 5.1 JSON 크기 한계 | `pwsh`(PS7)로 실행 |

**🔒 게이트 D2** — 2026-08-12 통과 (RDS).

---

## §7. S2 — WAS 담당

> **임계 경로. 최우선.** BFF·Web 2명이 대기 중이다.
> 계약은 README §6.5~§6.8. 여기서는 실행만 다룬다.

### 7.1 코드 정합화 (D1 대기 없이 즉시 시작)

| # | 작업 | 검증 |
|---|---|---|
| 1 | `JwtTokenProvider` / `createPatientToken` / `createStaffToken` **전량 삭제** | `grep -ri "JwtTokenProvider\|createPatientToken\|createStaffToken" src/` → 0건 |
| 2 | 로그인 응답을 `{actor_id, actor_type, role, name}` 토큰 없는 형태로 교체 | 단위 테스트 |
| 3 | `spring.jackson.property-naming-strategy: SNAKE_CASE` 적용 | `patientId → patient_id` 테스트 |
| 4 | JDBC URL을 **`sslMode=REQUIRED&serverTimezone=Asia/Seoul`** 로 교체 | `grep "useSSL"` → 0건 |
| 5 | `ActorHeaderFilter` — 경로↔`actor_type`/`role` 2차 검증 (불일치 403) | 단위 테스트 |
| 6 | 인증 3개 API는 `X-Actor-*` 요구 제외 | 단위 테스트 |
| 7 | `GlobalExceptionHandler` — `{error, message, trace_id}` 통일 | 테스트 |
| 8 | 계정 잠금: 5회 → `locked_until = now+30m`, 423 | 테스트 |
| 9 | 예약 상태를 **표준 enum** 으로 반환 (한글 라벨·DB 원본값 금지) | 테스트 |
| 10 | `/healthz`(프로세스) / `/readyz`(**DB 커넥션**) 구현 | curl 200 |
| 11 | 인증 API body 전체 로깅 제거 | 코드 검토 |
| 12 | 예약 슬롯 조회 시작점을 `max(조회일 00:00, now)` 로 | 지난 시각 슬롯 미반환 확인 |

> ⚠️ **8번** — 카운터 증가는 `@Transactional(REQUIRES_NEW)` 별도 빈(`LoginAttemptService`)으로 분리한다. 같은 트랜잭션에 두면 예외 롤백에 증가분이 함께 사라지고 **응답은 정상이라 어떤 시나리오도 못 잡는다.** 자기호출은 프록시를 타지 않으므로 반드시 다른 빈이어야 한다.

### 7.2 진료·처방 도메인 (7.1과 병렬 가능)

> **D2(ICD 적재)를 기다리지 않는다** — 로컬 MySQL에 소량 샘플만 넣고 개발한다.

| # | 작업 | 검증 |
|---|---|---|
| 1 | 엔티티 5종 매핑 — `IcdCode`, `IcdCodeSynonym`, `Chart`, `Prescription`, `PrescriptionItem` | `gradle build` |
| 2 | `GET /internal/staff/patients?date=` — 당일 예약 목록 | 단위 테스트 |
| 3 | `GET /internal/staff/icd-codes?q=` — **동의어 테이블 조회**, `q` 2자 미만 `400`, 상한 **50건** | 테스트 |
| 4 | `ChartService.create()` — 차트 + 처방 + 항목 **단일 `@Transactional`** | 부분 저장 없음 확인 |
| 5 | `icd_code` 미존재 시 `400 validation_error` | 테스트 |
| 6 | `chart.visit_no` UNIQUE 위반 → `409 duplicate_prescription` 변환 | **DB 제약을 잡아서 변환한다** |
| 7 | `icd_name_snapshot` — 발급 시점 `icd_code.name_kr` 복사 저장 | 테스트 |
| 8 | `patient_id` 를 **`visit_no` → `appointment` 조인으로 결정**. 본문 값 무시 | 위조 테스트 |
| 9 | `role != DOCTOR` 의 차트 생성 → **403** (WAS 2차 방어선) | NURSE 토큰 테스트 |
| 10 | `GET /internal/patient/prescriptions` — `X-Actor-Id` 기준 **본인 것만** | 타 환자 조회 404 |
| 11 | `GET /internal/staff/patients/{visit_no}/chart` — 차트+처방+항목 동시 반환, 미작성 404 | 테스트 |
| 12 | ICD 마스터에 **INSERT/UPDATE/DELETE 코드 없음** | `grep` 확인 |
| 13 | 외부 HTTP로 공공데이터포털 호출하는 코드 **없음** | `grep -ri "data.go.kr\|apis.data" src/` → **0건** |

> ⚠️ **6번이 핵심이다.** "먼저 SELECT 해서 있으면 409" 는 동시 요청 2건에서 둘 다 통과한다. `DataIntegrityViolationException` 을 잡아 409로 변환하는 것이 유일하게 옳은 구현이다. 예약의 `slot_taken` 과 동일 원칙.
> ⚠️ **13번** — README §13-20 금지 항목이다. 요구사항 문장("WAS가 API를 받아와 저장한다")을 그대로 구현하면 위반이다. 적재는 DB seed가 담당한다.

### 7.3 로컬 통합 검증

```powershell
cd local
.\prepare.ps1                           # digest·시크릿·mysql-init 생성
docker compose up --build -d --wait     # --wait 필수. 없으면 기동 전에 반환한다
.\verify-was.ps1                        # WAS 계약 검증 38개
```

> **게이트 D3** — `/readyz` → `{"status":"ok","db":"up"}` (= `ddl-auto: validate` 통과)
> **게이트 W2** — `PASS 38 / FAIL 0`
>
> DDL을 고쳤다면 `docker compose down -v` → `prepare.ps1` 재실행이 필수다. init 스크립트는 빈 볼륨에서만 돈다.

**검증 항목 요약**

| # | 항목 | 기대 |
|---|---|---|
| 1 | `/healthz` | 200 |
| 2 | `/readyz` | 200 (DB 연결됨) |
| 3 | `POST /internal/patient/auth/register` | 201 + `{actor_id, role, patient}` |
| 4 | 중복 `login_id` 재가입 | 409 `patient_exists` |
| 5 | `POST /internal/patient/auth/login` 정상 | 200 |
| 6 | 비밀번호 오류 | 401 `invalid_credentials` |
| 7 | 존재하지 않는 계정 | 401 `invalid_credentials` (**404 아님**) |
| 8 | 5회 연속 실패 후 6번째 | **423 `account_locked`** |
| 9 | `POST /internal/staff/auth/login` | 200 + `role=DOCTOR` |
| 10 | `X-Actor-Type=STAFF` 로 `/internal/patient/**` 호출 | **403** |
| 11 | 응답 JSON 필드가 전부 snake_case | 육안 + 테스트 |

> ⚠️ **전 항목이 `status=-1` 로 실패하면 애플리케이션 결함이 아니다.** `up -d --wait` 누락 등 연결 문제의 신호다. 항목별로 다르게 깨져야 애플리케이션 결함이다.

### 7.4 ECR 푸시 및 배포

```powershell
# ① digest 핀 확인 후 빌드
cd apps/was
$TAG = (git rev-parse --short HEAD)
$REG = "597106152264.dkr.ecr.ap-northeast-2.amazonaws.com"
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin $REG
docker build --platform linux/amd64 -t "${REG}/hybrid-toy/was:${TAG}" .
docker push "${REG}/hybrid-toy/was:${TAG}"

# ② Secret / ConfigMap
kubectl -n app create secret generic was-secret --from-literal=DB_USER=app_was --from-literal=DB_PASSWORD='<DB팀 발급>'
kubectl -n app apply -f ../../k8s/was/configmap.yaml

# ③ 배포
kubectl -n app apply --server-side --force-conflicts -f ../../k8s/was/deployment.yaml -f ../../k8s/was/service.yaml
kubectl -n app rollout status deploy/was
```

**검증**

```powershell
kubectl -n app get pod -l app=was
kubectl -n app logs deploy/was --tail=50
kubectl -n app get endpoints was-svc                        # 비어 있으면 셀렉터 불일치
kubectl -n app run tmp --rm -it --image=curlimages/curl --restart=Never -- curl -s -o /dev/null -w "%{http_code}\n" http://was-svc.app.svc.cluster.local:8080/readyz
```

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| `CrashLoopBackOff` + `Communications link failure` | JDBC TLS 옵션 누락 |
| `exec format error` | `--platform linux/amd64` 미지정 |
| `ImagePullBackOff` | 태그 오타 / 푸시 미완료 |
| `/readyz` 503 | DB 커넥션 실패. Secret 값 확인 |
| `endpoints` 비어 있음 | Service `selector` ↔ Pod `labels` 불일치 |
| 파드가 0개, `describe` 가 전부 빔 | **ResourceQuota 소진.** `kubectl -n app describe resourcequota` |

**🔒 게이트 W1** — `was-svc:8080/readyz` → **200**. BFF 담당에게 즉시 통보.

---

## §8. S3 — BFF 담당

> W1 통과 후 배포. **코드 작업은 W1 대기 없이 즉시 시작한다.**
> 계약은 README §6.5~§6.7.

### 8.1 코드 구현

| # | 작업 | 검증 |
|---|---|---|
| 1 | Cognito 의존성·코드 전량 제거 | `grep -ri cognito` → 0건 |
| 2 | `jjwt` 제거, Nimbus(`JwtEncoder`/`JwtDecoder`) 채택 — **버전 `10.9.1` 명시 핀** | `build.gradle.kts` 확인 |
| 3 | HS256 + `JWT_SIGNING_KEY` 환경변수 주입 | 기동 확인 |
| 4 | **`TraceIdFilter`** — UUID 생성, 외부 `X-Trace-Id` 무시, 응답 헤더 부착 | 테스트 |
| 5 | **`InboundActorHeaderStripFilter`** — 외부 `X-Actor-*` 제거 (**필터 체인 최상단**) | 위조 헤더 테스트 |
| 6 | `GET /api/bff/patient/auth/csrf` + CSRF 보호 (POST/PATCH/DELETE) | 테스트 |
| 7 | `PATIENT_TOKEN` 쿠키 — HttpOnly/Secure/Lax/**Path=`/api/bff/patient`**/1800s | `Set-Cookie` 확인 |
| 8 | `XSRF-TOKEN` 쿠키 — **Path=`/`** (JS가 읽어야 한다) | 브라우저 `document.cookie` 확인 |
| 9 | 전달수단↔`actor_type` 바인딩 검증 | **4케이스** (아래) |
| 10 | 경로 제한 — PATIENT는 `/patient/**`, STAFF는 `/staff/**` | 테스트 |
| 11 | `role` → `ROLE_*` 권한 변환 | 테스트 |
| 12 | `WasClient` — **`JdkClientHttpRequestFactory`**, connect 2s / read 5s / **retry 없음** | 장애 주입 |
| 13 | WAS 5xx·timeout → **503 `upstream_unavailable`** 변환 | 장애 주입 |
| 14 | WAS 400/401/404/409/423 → 그대로 전달 | 테스트 |
| 15 | 쿼리 값은 **URI 템플릿 변수**로만 전달 (사전 인코딩 금지) | 한글 검색 테스트 |
| 16 | JWT 발급 시 `X-Actor-*` 재생성 후 WAS 전달 | 테스트 |
| 17 | `/healthz`, `/readyz` (**WAS에 종속시키지 않음**) | curl 200 |
| 18 | 직원 JWT TTL **8시간**, 환자 **30분** | 디코딩 확인 |
| 19 | 오류 응답에서 `response.reset()` **미사용** | `X-Trace-Id` 헤더 잔존 확인 |

**9번 — 바인딩 4케이스 (전부 401이어야 한다)**

| 케이스 | 기대 |
|---|---|
| STAFF JWT를 `PATIENT_TOKEN` 쿠키로 전달 | 401 |
| PATIENT JWT를 `Authorization: Bearer` 로 전달 | 401 |
| `actor_type` claim 없는 JWT | 401 |
| 알 수 없는 `actor_type` | 401 |

> ⚠️ **Spring Boot 4 / Security 7 이관 시 컴파일이 4회 막혔다.** 전부 예상된 실패이며 그 자리에서 닫았다. 유형은 README §11.3 참조 — Nimbus BOM 미관리 / `AntPathRequestMatcher` 제거 / `starter-restclient` 분리 / Jackson 3 프로퍼티 이동.

### 8.2 로컬 전체 통합

```powershell
cd local
docker compose up --build -d --wait     # mysql + was + bff + patient-web
.\verify-bff.ps1                        # BFF 계약 검증 45개
```

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | `GET /api/bff/patient/auth/csrf` | 200 + `XSRF-TOKEN` 쿠키 |
| 2 | CSRF 헤더 없이 register | 403 |
| 3 | CSRF 헤더 포함 register | 201 + `PATIENT_TOKEN` 쿠키 |
| 4 | 쿠키 없이 `GET /appointments` | 401 |
| 5 | 쿠키 포함 `GET /appointments` | 200 |
| 6 | 요청에 `X-Actor-Id: 999` 위조 삽입 | **무시됨** (WAS 로그에 999 없음) |
| 7 | WAS 컨테이너 정지 후 API 호출 | **503 `upstream_unavailable`** |
| 8 | WAS 재기동 후 재호출 | 200 (복구) |
| 9 | 직원 로그인 → Bearer로 `/staff/patients` | 200 |
| 10 | NURSE 토큰으로 `POST /staff/charts` | **403** |
| 11 | 오류 응답 3필드 + `X-Trace-Id` 헤더 | 전부 존재 |
| 12 | BFF 로그와 WAS 로그의 `trace_id` 일치 | 동일 UUID |
| 13 | 한글 `?q=당뇨` 검색 | BFF 경유 결과 = WAS 직접 결과 |

**🔒 게이트 B1 / B2** — `PASS 45 / FAIL 0`. Web 담당에게 통보.

### 8.3 배포

```powershell
cd apps/bff
$TAG = (git rev-parse --short HEAD)
$REG = "597106152264.dkr.ecr.ap-northeast-2.amazonaws.com"
docker build --platform linux/amd64 -t "${REG}/hybrid-toy/bff:${TAG}" .
docker push "${REG}/hybrid-toy/bff:${TAG}"

kubectl -n app create secret generic bff-secret --from-literal=JWT_SIGNING_KEY='<리드 발급>'
kubectl -n app apply -f ../../k8s/bff/configmap.yaml
kubectl -n app apply --server-side --force-conflicts -f ../../k8s/bff/deployment.yaml -f ../../k8s/bff/service.yaml
kubectl -n app rollout status deploy/bff
```

**검증**

```powershell
kubectl -n app run tmp --rm -it --image=curlimages/curl --restart=Never -- sh -c "curl -s -o /dev/null -w '%{http_code}\n' http://bff-svc.app.svc.cluster.local:8080/healthz; curl -s -i http://bff-svc.app.svc.cluster.local:8080/api/bff/patient/auth/csrf | head -20"
```

---

## §9. S4 — Web 담당

> B1 통과 후. **이미지 빌드는 그 전에 끝내 둔다.**

| # | 작업 | 검증 |
|---|---|---|
| 1 | `nginx.conf` — `listen 8080`, non-root | 로컬 curl |
| 2 | `/healthz`, `/readyz` 정적 200 | 로컬 curl |
| 3 | SPA fallback (`try_files ... /index.html`) | 새로고침 시 404 안 남 |
| 4 | **`location /api/bff/staff { return 404; }`** 명시 | curl 404 |
| 5 | `charset utf-8; charset_types ...` 설정 | 응답 헤더에 charset 존재 |
| 6 | API 호출은 상대경로 `/api/bff/patient` (도메인 하드코딩 없음) | `grep -r "https://"` |
| 7 | `credentials: "include"` 적용 | 코드 검토 |
| 8 | **CSRF 토큰을 매 요청 직전 재조회** (캐시 금지) | 연속 POST 3회 테스트 |
| 9 | 이미지 빌드·푸시 | ECR 확인 |
| 10 | Deployment + Service 배포 | Pod Running |
| 11 | **Ingress apply** | 아래 |
| 12 | ALB DNS명 확보 → 리드 전달 | — |

**11번 — Ingress (README §6.9 확정본 사용)**

```powershell
# ACM ARN을 손으로 복사하지 않는다 — CLI 로 받는다
aws acm list-certificates --region ap-northeast-2 `
  --query "CertificateSummaryList[].{Domain:DomainName,Arn:CertificateArn}" --output table

$ACM = (aws acm list-certificates --region ap-northeast-2 --query "CertificateSummaryList[0].CertificateArn" --output text)
```

치환은 §3의 `Set-Placeholder` 로만 한다. 이후:

```powershell
kubectl apply --server-side --force-conflicts -f ..\..\k8s\ingress\
kubectl -n app get ingress patient-web -w      # ADDRESS 표시까지 2~4분
```

**🚨 적용 직후 반드시 대조 — ARN 절단 3회차 방지**

```powershell
kubectl -n app get ingress patient-web `
  -o jsonpath="{.metadata.annotations.alb\.ingress\.kubernetes\.io/certificate-arn}"
```

`certificate/` 뒤 36자 UUID가 온전해야 한다.

**ALB DNS 확보 → 리드 전달**

```powershell
kubectl -n app get ingress patient-web -o jsonpath="{.status.loadBalancer.ingress[0].hostname}"
```

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| ADDRESS가 영원히 빈칸 | LBC 로그 확인. IRSA 권한 / 서브넷 태그 |
| Target Group unhealthy | `/healthz` 미구현 또는 경로 불일치 |
| API 요청이 Web으로 감 | Ingress에서 `/` 를 먼저 선언 → **`/` 를 마지막으로** |
| ALB가 인증서 오류 | ACM ARN 절단 |

---

## §10. S5 — EKS 배포 → 🚩 G2

### 10.1 사전 준비 (1회)

| # | 작업 | 담당 |
|---|---|---|
| 1 | `aws eks update-kubeconfig --name hybrid-toy-eks --region ap-northeast-2` | 각자 |
| 2 | `kubectl apply --server-side -f k8s/namespace/` | EKS팀 |
| 3 | `was-secret` 생성 (`DB_USER`, `DB_PASSWORD`) | EKS팀 |
| 4 | `bff-secret` 생성 (`JWT_SIGNING_KEY`) | EKS팀 |
| 5 | `apps/patient-web/Dockerfile` digest 치환 | EKS팀 |
| 6 | `k8s/ingress/` ACM ARN 치환 | EKS팀 |

**3~4번 — 값을 명령행에 남기지 않는다**

```powershell
$pw = Read-Host "app_was 비밀번호" -AsSecureString
$pt = [Runtime.InteropServices.Marshal]::PtrToStringBSTR(
        [Runtime.InteropServices.Marshal]::SecureStringToBSTR($pw))
kubectl -n app create secret generic was-secret `
  --from-literal=DB_USER=app_was --from-literal=DB_PASSWORD=$pt
$pt = $null

$k = (aws secretsmanager get-secret-value --secret-id bff-secret --region ap-northeast-2 `
       --query SecretString --output text | ConvertFrom-Json).JWT_SIGNING_KEY
kubectl -n app create secret generic bff-secret --from-literal=JWT_SIGNING_KEY=$k
$k = $null
```

### 10.2 사전 점검

```powershell
cd scripts
.\preflight.ps1
```

**17개 항목을 본다. FAIL이 남은 채 배포하지 않는다.** 파드가 CrashLoopBackOff로 돌기 시작하면 원인이 코드인지 설정인지 구분되지 않는다.

> `cloudflared digest 치환됨` FAIL은 G3 전까지 정상이다.

### 10.3 배포 — 의존 역순

```powershell
.\deploy.ps1 -Service was          -Action all
.\deploy.ps1 -Service bff          -Action all
.\deploy.ps1 -Service patient-web  -Action all
```

⚠️ **커밋하지 않으면 스크립트가 중단한다.** 태그가 git short SHA이므로 미커밋 상태로 빌드하면 이미지와 커밋이 어긋나 추적이 불가능해진다.

⚠️ **ECR은 immutable tag다.** 같은 태그를 두 번 푸시할 수 없다. 배포할 때마다 §18 표에 태그를 기록한다.

### 10.4 리드 작업 (Cloudflare)

| # | 작업 | 검증 |
|---|---|---|
| 1 | SSL/TLS = **Full (strict)** | 대시보드 |
| 2 | `www` CNAME → ALB DNS, **🟠 주황 구름 ON** | 대시보드 |
| 3 | DNS 전파 | `nslookup www.kuspitalsoldeskproject.org` |
| 4 | WAF Managed Ruleset / Rate Limiting / Bot Fight Mode | 대시보드 |

### 10.5 🔒 게이트 G2

**① 스크립트**

```powershell
.\verify-flow1.ps1        # PASS 24 / FAIL 0
```

**② 브라우저 — 이것이 최종 조건이다**

- [x] `https://www.kuspitalsoldeskproject.org` 화면 표시
- [x] **가입 → 403이 나지 않음**
- [x] 로그인 상태 표시 (우상단 이름)
- [x] 의사·날짜 선택 → 시간 슬롯 표시
- [x] 예약 → 접수번호 `V20260812-XXXX`
- [x] 내 예약에 카드 표시
- [x] **새로고침 후 로그인 유지**
- [x] 로그아웃 → 새로고침 시 가입 화면
- [x] 콘솔에 403·CORS 오류 없음 (초기 401 1회는 정상 — README §16.4)
- [x] Cloudflare Analytics에 트래픽 기록

> 🚨 **스크립트만으로 G2를 닫지 않는다.**
> `verify-bff` 45개 + `verify-flow1` 22개를 통과한 상태에서 브라우저 회원가입이 403으로 실패한 이력이 있다.
> 원인은 CSRF 쿠키 `Path` 였고, PowerShell `CookieContainer` 는 경로를 지정해 조회하므로 이 결함을 통과시켰다.
> **하네스는 브라우저보다 관대하다** (README §12.4).

**🔒 게이트 G2** — 2026-08-12 통과 (24/24 + 브라우저).

---

## §11. S6 — 내부 흐름 → 🚩 G3

### 11.1 리드 (Cloudflare)

| # | 작업 | 산출물 → 전달 |
|---|---|---|
| 1 | Tunnel 생성 | Tunnel Token → BFF |
| 2 | Public Hostname: `staff-api.kuspitalsoldeskproject.org` → `http://bff-svc.app.svc.cluster.local:8080` | — |
| 3 | Access Application 생성 (위 도메인) | — |
| 4 | Policy **Action = `Service Auth`** | — |
| 5 | Service Token 발급 | ID + Secret → OKD |
| 6 | Tunnel에 **Protect with Access 활성화 + 올바른 AUD** | — |

### 11.2 BFF 담당 (cloudflared)

```powershell
kubectl -n app create secret generic cloudflared-secret --from-literal=TUNNEL_TOKEN='<리드 발급>'
kubectl -n app apply --server-side --force-conflicts -f k8s/cloudflared/deployment.yaml
kubectl -n app rollout status deploy/cloudflared
kubectl -n app logs deploy/cloudflared --tail=30
```

**검증**: 로그에 터널 등록 성공 + Cloudflare 대시보드 Tunnel **HEALTHY** (replica 2 모두).

**실행 결과** — Tunnel이 4커넥션(icn05·icn06 이중화)으로 등록됐고, cloudflared 파드 2/2 Running, 대시보드 HEALTHY 확인.

### 11.3 실행 순서 (실제로 통한 순서)

```
리드(Tunnel·Access 구성 → Service Auth 정책 → Service Token 발급)
  → EKS팀(digest 치환 → cloudflared-secret → 배포 → preflight 17/17)
  → verify-flow1.ps1 (차트 미작성 예약 확보)
  → verify-flow2.ps1
```

> `verify-flow2` 는 "차트가 아직 작성되지 않은 예약"을 필요로 한다. `verify-flow1` 을 먼저 돌려 예약을 만들어 두어야 한다.

### 11.4 OKD팀

| # | 작업 |
|---|---|
| 1 | `cf-access-token` Secret 생성 |
| 2 | Web Pod 환경변수 주입 (`BFF_BASE_URL` + Access 2종) |
| 3 | 서버사이드 프록시 구현 — Access 헤더 부착 |
| 4 | 직원 로그인 → JWT를 **서버사이드 세션**에 보관 |
| 5 | 업무 API 호출 시 `Authorization: Bearer` 추가 |
| 6 | 표준 enum → 직원 화면 한글 라벨 변환 |
| 7 | 로그아웃 시 세션·JWT 삭제 |

```bash
oc create secret generic cf-access-token \
  --from-literal=CF_ACCESS_CLIENT_ID='<CLIENT_ID>' \
  --from-literal=CF_ACCESS_CLIENT_SECRET='<CLIENT_SECRET>' \
  -n <내부-네임스페이스>
```

```yaml
spec:
  template:
    spec:
      containers:
        - name: internal-web
          env:
            - name: BFF_BASE_URL
              value: "https://staff-api.kuspitalsoldeskproject.org"
            - name: CF_ACCESS_CLIENT_ID
              valueFrom:
                secretKeyRef: { name: cf-access-token, key: CF_ACCESS_CLIENT_ID }
            - name: CF_ACCESS_CLIENT_SECRET
              valueFrom:
                secretKeyRef: { name: cf-access-token, key: CF_ACCESS_CLIENT_SECRET }
```

> ⚠️ **OKD Web 애플리케이션 구현은 프로젝트 기간 제약으로 범위 밖 이관됐다.** 위 1~2번(인프라 측 준비)까지가 완료 범위이며, 3~7번은 미구현이다. 흐름2의 관통 검증은 `verify-flow2.ps1` 이 OKD Web을 대신해 수행했다.

### 11.5 단계별 진단 (이 순서로 좁힌다)

```bash
# ① Access 통과 여부만 확인 — 401 JSON이면 통과한 것
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" \
  https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/patients \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>"

# ② 직원 로그인
curl -s -X POST https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/auth/login \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>" \
  -H "Content-Type: application/json" \
  -d '{"login_id":"doc_kim","password":"<seed 비밀번호>"}'

# ③ Bearer로 업무 API
curl -s https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/patients \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>" \
  -H "Authorization: Bearer <STAFF_JWT>"
```

| ①의 결과 | 판정 | 조치 주체 |
|---|---|---|
| `200` / `401` + `application/json` | ✅ Access 통과. BFF까지 도달 | — |
| `302` 또는 `200 text/html` | ❌ Policy Action이 `Allow` | 리드 |
| `403` | ❌ Service Token 값 오류 | OKD |
| 연결 실패 | ❌ 아웃바운드 443 / DNS / 프록시 | OKD |
| `502` / `504` | ❌ cloudflared Pod 또는 `bff-svc` 문제 | BFF |

> ⚠️ **`403` 응답의 `Content-Type` 은 `text/html` 이다** (Cloudflare 자체 오류 페이지). 이를 "Allow 오설정 신호"로 오판하지 않는다. 판정은 **상태코드만** 본다.

### 11.6 🔒 게이트 G3 — 2026-08-13 통과 (`verify-flow2.ps1` 29/29)

- [x] Access 통과 → BFF 도달 (`401 application/json`)
- [x] 직원 로그인 → Bearer JWT (exp = 8시간), 3역할 전부
- [x] DOCTOR가 차트+처방 작성 **201** / NURSE **403**
- [x] 동일 `visit_no` 재발급 → **409 `duplicate_prescription`**
- [x] ADMIN_STAFF만 `/staff/admin/**` 접근 (경로 미구현이라 `404`, `403` 아님)
- [x] 잘못된 Service Token → **403**
- [x] 위조 서명 JWT → **401**
- [x] 위조 `X-Actor-*` 헤더 주입 → **401**
- [x] `chart.icd_name_snapshot` 이 발급 시점 명칭으로 저장
- [x] `X-Trace-Id` 가 BFF → WAS 전 구간 동일

**실행 중 발견 — 검증 스크립트 자체 결함 1건, 인프라는 처음부터 정상이었다**

최초 실행에서 2건 FAIL:

```
[FAIL] Service Token 없이 접근 차단        status=403
[FAIL] 잘못된 Service Token → 403         status=403
```

상태코드는 기대값과 정확히 일치했다. 원인은 판정 로직에 `Content-Type` 조건을 같이 걸어둔 것 — **Cloudflare Access의 차단 응답 자체가 `text/html`** 인데 이를 "Policy가 Allow일 때의 로그인 폼 신호"와 혼동했다.

`Allow` 오설정이었다면 `302` 또는 `200`(로그인 폼)이 왔을 것이나 실제로는 두 경우 모두 **403** 이었다 — Service Auth가 처음부터 정확히 동작하고 있었다.

교정: 판정을 **상태코드만**으로 좁히고, Allow 오탐지는 `302`/`200` 여부를 보는 별도 항목으로 분리(29개로 증가). 재실행 후 인프라 재작업 없이 **29/29**.

> **교훈** — 검증 실패를 발견하면 먼저 "무엇을 기대했는가"를 계약과 대조한다 (README §12.5).

---

## §12. S7 — 관측성 구축

> G3 통과 후 착수. 설계 판단은 README §7.13 / §7.14, 계약·운영은 README §10.
> **소요 약 40분.** 여기서는 실행 절차만 다룬다.

### 12.1 Phase 0 — 사전 확인

```powershell
$CLUSTER = "hybrid-toy-eks"
$REGION  = "ap-northeast-2"

aws sts get-caller-identity --query Arn --output text
aws eks describe-addon --cluster-name $CLUSTER --addon-name eks-pod-identity-agent `
  --region $REGION --query "addon.status" --output text
kubectl get nodes -o wide
kubectl describe node | Select-String -Pattern "Allocated resources" -Context 0,8
```

| 항목 | 기대값 | 실패 시 |
|---|---|---|
| ARN | `.../user/team03` | 프로파일 교체 |
| `eks-pod-identity-agent` | `ACTIVE` | Pod Identity 경로 불가 → IRSA로 우회 |
| 노드 | `Ready` × 2 | 진행 중단 |
| 노드 CPU requests | 여유 확인 | 아래 |

**실측 결과** (2026-08-13)

| 노드 | CPU requests | 판정 |
|---|---|---|
| `ip-10-0-10-156` | 900m (46%) | 타이트하나 통과 가능 |
| `ip-10-0-11-185` | 600m (31%) | 여유 |

애드온이 추가하는 파드: `controller-manager`(1) + `cloudwatch-agent`(노드당 1) + `fluent-bit`(노드당 1) = **총 5개**.
DaemonSet 2종이 얹히면 `ip-10-0-10-156` 의 requests가 allocatable의 80%대까지 오른다.

> ⚠️ **Cluster Autoscaler 미설치 상태라 노드그룹 `max=3` 이 비활성이다.** `Pending` 이 뜨면 자동 증설되지 않는다.
> 사전에 노드를 늘리지 않고, **실패 시에만** 대응한다 — 관측성 도입 자체가 비용 관찰 대상이므로 불필요한 노드를 먼저 만들지 않는다.

```powershell
# Pending 발생 시에만 실행. <ng-name> 은 아래로 조회한 실제 이름으로 치환
aws eks list-nodegroups --cluster-name $CLUSTER --region $REGION --query "nodegroups" --output text

aws eks update-nodegroup-config --cluster-name $CLUSTER --nodegroup-name <ng-name> `
  --scaling-config minSize=1,maxSize=3,desiredSize=3 --region $REGION
```

> ⚠️ PowerShell에서 `<ng-name>` 을 그대로 두면 `'<' 연산자는 나중에 사용하도록 예약되어 있습니다` 파서 오류가 난다. 리다이렉션 연산자로 해석되기 때문이다. **placeholder는 반드시 치환한다.**

**AL2023 확인** — `Amazon Linux 2023.x`. dataplane/host 로그가 기본 수집되지 않는다(README §10.2). 시연 범위에 영향 없으나 사실로 알고 있을 것.

### 12.2 Phase 1 — IAM Role (Pod Identity)

```powershell
cd C:\Users\hongj\Desktop\lastdance-main
mkdir monitoring -Force
cd monitoring

$trust = @'
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": { "Service": "pods.eks.amazonaws.com" },
      "Action": ["sts:AssumeRole", "sts:TagSession"]
    }
  ]
}
'@
[System.IO.File]::WriteAllText("$PWD\cw-trust.json", $trust, (New-Object System.Text.UTF8Encoding($false)))
```

**BOM 확인 — 생략 금지.** AWS CLI는 BOM 붙은 JSON을 파싱하지 못한다.

```powershell
Format-Hex .\cw-trust.json | Select-Object -First 1
# 첫 바이트가 7B ('{') 여야 한다. EF BB BF 면 실패 — 재작성
```

```powershell
aws iam create-role `
  --role-name hybrid-toy-eks-cwagent-role `
  --assume-role-policy-document file://cw-trust.json `
  --tags Key=Project,Value=hybrid-toy

aws iam attach-role-policy `
  --role-name hybrid-toy-eks-cwagent-role `
  --policy-arn arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy

$CW_ROLE_ARN = aws iam get-role --role-name hybrid-toy-eks-cwagent-role --query "Role.Arn" --output text
$CW_ROLE_ARN
```

### 12.3 Phase 2 — 애드온 구성값

```powershell
$cfg = @'
{
  "agent": {
    "config": {
      "logs": {
        "metrics_collected": {
          "kubernetes": {
            "enhanced_container_insights": true,
            "accelerated_compute_metrics": false
          }
        }
      }
    }
  },
  "manager": {
    "applicationSignals": {
      "autoMonitor": {
        "monitorAllServices": false
      }
    }
  }
}
'@
[System.IO.File]::WriteAllText("$PWD\cw-addon-config.json", $cfg, (New-Object System.Text.UTF8Encoding($false)))
```

**버전 조회 — 하드코딩하지 않는다** (README §5.5)

```powershell
aws eks describe-addon-versions `
  --addon-name amazon-cloudwatch-observability `
  --kubernetes-version 1.35 `
  --query "addons[0].addonVersions[?compatibilities[0].defaultVersion==``true``].addonVersion" `
  --output text

$CW_ADDON_VER = "v6.5.0-eksbuild.1"      # 위 출력값
```

> Pod Identity는 v3.1.0+, `autoMonitor` 는 v5.0.0+ 에서 유효하다. v5 미만이면 `manager` 블록을 제거하고 애플리케이션 재배포 금지를 팀에 공지한다.

### 12.4 Phase 3 — 애드온 설치

```powershell
aws eks create-addon `
  --cluster-name $CLUSTER `
  --addon-name amazon-cloudwatch-observability `
  --addon-version $CW_ADDON_VER `
  --pod-identity-associations "serviceAccount=cloudwatch-agent,roleArn=$CW_ROLE_ARN" `
  --configuration-values file://cw-addon-config.json `
  --region $REGION
```

```powershell
do {
  $s = aws eks describe-addon --cluster-name $CLUSTER `
        --addon-name amazon-cloudwatch-observability `
        --region $REGION --query "addon.status" --output text
  Write-Host "status=$s"
  Start-Sleep -Seconds 15
} while ($s -eq "CREATING")
```

**실측 소요** — `CREATING` → `ACTIVE` **약 45초**.

**실패 시**

```powershell
aws eks describe-addon --cluster-name $CLUSTER `
  --addon-name amazon-cloudwatch-observability --region $REGION `
  --query "addon.health.issues"
```

| 에러 문구 | 원인 | 조치 |
|---|---|---|
| `Conflicts found when trying to apply` | 기존 CW agent 잔재 | `--resolve-conflicts OVERWRITE` 재시도 |
| `InsufficientNumberOfReplicas` / 파드 `Pending` | 노드 리소스 부족 | §12.1의 desiredSize=3 |
| JSON parse 오류 | 구성 파일 BOM | §12.2 방식으로 재작성 |

### 12.5 Phase 4 — 클러스터 측 검증

```powershell
kubectl get pods -n amazon-cloudwatch -o wide
```

기대 (노드 2개 기준):

```
amazon-cloudwatch-observability-controller-manager-xxxxx  1/1 Running
cloudwatch-agent-xxxxx                                    1/1 Running
cloudwatch-agent-yyyyy                                    1/1 Running
fluent-bit-xxxxx                                          1/1 Running
fluent-bit-yyyyy                                          1/1 Running
```

```powershell
# ① Pod Identity 연결 확인
kubectl get sa cloudwatch-agent -n amazon-cloudwatch -o yaml
aws eks list-pod-identity-associations --cluster-name $CLUSTER --region $REGION `
  --query "associations[?serviceAccount=='cloudwatch-agent']"

# ② 전송 실패 여부 — 출력이 없어야 정상
kubectl logs -n amazon-cloudwatch -l app.kubernetes.io/name=cloudwatch-agent --tail=50 |
  Select-String -Pattern "AccessDenied|ExpiredToken|NoCredentialProviders|error"

# ③ 합격 조건 — app 네임스페이스 파드 불변 확인
kubectl get pods -n app
kubectl get deploy -n app -o jsonpath="{range .items[*]}{.metadata.name}{'\t'}{.spec.template.spec.initContainers[*].name}{'\n'}{end}"
```

> ✅ **③이 이 단계의 합격 조건이다.** `app` 파드의 `RESTARTS` 가 증가하지 않고, initContainer 목록이 비어 있어야 한다.
> 무언가 주입됐다면 Application Signals 차단이 실패한 것 — 즉시 `aws eks delete-addon` 후 §12.3으로 복귀한다.

**실측 결과** — 5개 파드 전부 Running, 에이전트 로그에 오류 없음, `app` 파드 8개 RESTARTS 0 유지, initContainer 미주입 확인.

### 12.6 Phase 5 — 로그 보존기간 설정 (생략 금지)

Fluent Bit은 로그 그룹을 **보존기간 무제한** 으로 자동 생성한다. 생성까지 5~10분 소요.

```powershell
aws logs describe-log-groups `
  --log-group-name-prefix "/aws/containerinsights/$CLUSTER" `
  --region $REGION `
  --query "logGroups[].{Name:logGroupName,Retention:retentionInDays}" --output table
```

```powershell
$groups = aws logs describe-log-groups `
  --log-group-name-prefix "/aws/containerinsights/$CLUSTER" `
  --region $REGION --query "logGroups[].logGroupName" --output text

foreach ($g in ($groups -split "\s+" | Where-Object { $_ })) {
  aws logs put-retention-policy --log-group-name $g --retention-in-days 7 --region $REGION
  Write-Host "retention set: $g"
}
```

**적용 결과** — 4개 그룹(`application` / `performance` / `dataplane` / `host`) 전부 `None` → **7일**.

> 🚨 **이 시점부터 애플리케이션 로그가 외부에 영속 저장된다.** §2.3 "비밀번호·토큰 로그 출력 금지" 위반이 있었다면 여기서 실체화된다. **도입 직후 반드시 감사한다.**

### 12.7 Phase 6 — 대시보드 확인

CloudWatch 콘솔 → **Insights → Container Insights** → `ap-northeast-2` → `hybrid-toy-eks`

| 뷰 | 확인 항목 |
|---|---|
| Cluster 목록 | `hybrid-toy-eks` 표시, 노드/파드 수 일치 |
| Performance → EKS Nodes | 노드별 CPU/Memory/Network |
| → EKS Pods | `app` 의 was/bff/patient-web/cloudflared |
| → EKS Namespaces | `app`, `kube-system`, `amazon-cloudwatch` |

로그–메트릭 상관 확인 (Logs Insights, 그룹 `/aws/containerinsights/hybrid-toy-eks/application`):

```
fields @timestamp, kubernetes.pod_name, log
| filter kubernetes.namespace_name = "app"
| sort @timestamp desc
| limit 50
```

> 메트릭이 안 보이면 **10분 더 기다린다.** 첫 수집 반영에 지연이 있다. 그 후에도 없으면 §12.5 ②로 돌아간다.

**🔒 게이트 M1** — 2026-08-13 통과.

- [x] CW 파드 5개 Running
- [x] Pod Identity association 생성 확인
- [x] 에이전트 로그에 자격증명 오류 없음
- [x] `app` 파드 스펙 불변 (Application Signals 차단 성공)
- [x] 로그 그룹 4종 생성 + 보존 7일 적용
- [x] Container Insights 대시보드 표시

### 12.8 미완료 / 미착수

| 항목 | 상태 |
|---|---|
| CloudWatch Alarm | **미착수.** README §16.3 백로그 |
| `metrics-server` (`kubectl top`) | 미설치 |
| EKS 컨트롤플레인 로그 | 미활성 |
| OKD 관측성 통합 | **미통합 확정** (README §10.6) |
| `amazoncloudwatchagent` CR의 `VERSION 0.0.0` 표시 | 미조사. 파드는 정상 Running |

---

## §13. 통합 검증 시나리오

### 13.1 검증 스크립트 일람

| 스크립트 | 대상 | 건수 | 게이트 |
|---|---|---|---|
| `local/verify-was.ps1` | WAS 직접 (18080) | **38** | W2 |
| `local/verify-bff.ps1` | BFF 경유 (18081) | **45** | B2 |
| `scripts/verify-flow1.ps1` | 도메인 경유 (HTTPS) | **24** | G2 |
| `scripts/verify-flow2.ps1` | Tunnel 경유 | **29** | G3 |
| `scripts/preflight.ps1` | 배포 전 점검 | **17** | — |

> 로컬 compose뿐 아니라 **EKS에도 그대로 쓴다.**
> `kubectl -n app port-forward svc/bff-svc 18081:8080` 후 `verify-bff.ps1` 을 실행하면 포트가 같아 인자 없이 동작한다.

### 13.2 흐름1 — 외부 환자 (G2)

```powershell
$BASE = "https://www.kuspitalsoldeskproject.org"
$s = New-Object Microsoft.PowerShell.Commands.WebRequestSession

# 1. 화면
(Invoke-WebRequest "$BASE/" -WebSession $s).StatusCode                       # 200

# 2. CSRF
$csrf = Invoke-RestMethod "$BASE/api/bff/patient/auth/csrf" -WebSession $s
$hdr  = @{ "X-XSRF-TOKEN" = $csrf.token }

# 3. 회원가입
$body = @{ login_id="testpt01"; password="P@ssw0rd123"; name="테스트"; birth_date="1990-03-14" } | ConvertTo-Json
Invoke-RestMethod "$BASE/api/bff/patient/auth/register" -Method Post -Body $body -ContentType "application/json" -Headers $hdr -WebSession $s

# 4. 인증 후 조회
Invoke-RestMethod "$BASE/api/bff/patient/appointments" -WebSession $s

# 5. 직원 API 차단 확인 — 404 여야 정상
try { Invoke-WebRequest "$BASE/api/bff/staff/auth/login" -Method Post } catch { $_.Exception.Response.StatusCode.value__ }
```

| # | 항목 | 기대 |
|---|---|---|
| 1 | 화면 로드 | 200 |
| 2 | CSRF | 200 + `XSRF-TOKEN` 쿠키 |
| 3 | 회원가입 | 201 + `PATIENT_TOKEN` 쿠키 |
| 4 | 인증 API | 200 |
| 5 | 직원 API | **404** |
| 6 | 5회 오입력 후 | **423** |
| 7 | 타 환자 데이터 접근 | **404** |
| 8 | 로그아웃 후 재호출 | **401** |

> ⚠️ **CSRF 토큰은 매 요청 직전 다시 읽는다.** 최초 1회만 읽어 캐시하면 갱신 후 전부 403이 된다.

### 13.3 진료·처방 시나리오 (G3, Tunnel 경유)

```powershell
$BASE = "https://staff-api.kuspitalsoldeskproject.org"
$CF   = @{ "CF-Access-Client-Id" = "<ID>"; "CF-Access-Client-Secret" = "<SECRET>" }

# 1. 의사 로그인
$doc = Invoke-RestMethod "$BASE/api/bff/staff/auth/login" -Method Post -Headers $CF `
  -ContentType "application/json" -Body '{"login_id":"doc_kim","password":"<seed>"}'
$H = $CF + @{ "Authorization" = "Bearer $($doc.access_token)" }

# 2. 당일 예약 목록
$list = Invoke-RestMethod "$BASE/api/bff/staff/patients" -Headers $H
$visit = $list[0].visit_no

# 3. 상병코드 검색
Invoke-RestMethod "$BASE/api/bff/staff/icd-codes?q=당뇨" -Headers $H | Measure-Object

# 4. 차트 + 처방 발급
$body = @{
  visit_no = $visit
  icd_code = "E1140"
  chief_complaint = "다뇨, 하지 저림"
  note = "혈당 조절 불량. 2주 후 재내원."
  prescription_items = @(
    @{ drug_name = "메트포르민정 500mg"; dosage = "1정"; frequency = "1일 2회"; duration_days = 14 }
  )
} | ConvertTo-Json -Depth 5
Invoke-RestMethod "$BASE/api/bff/staff/charts" -Method Post -Headers $H -ContentType "application/json" -Body $body

# 5. 차트 재조회
Invoke-RestMethod "$BASE/api/bff/staff/patients/$visit/chart" -Headers $H
```

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | 당일 예약 목록 조회 | 200 + `visit_no` 포함 |
| 2 | `?q=당뇨` 검색 | 200 + **50건 이하** |
| 3 | `?q=E` (1자) | **400 `validation_error`** |
| 4 | DOCTOR 차트+처방 발급 | **201** |
| 5 | 동일 `visit_no` 재발급 | **409 `duplicate_prescription`** |
| 6 | NURSE 토큰으로 발급 | **403** |
| 7 | 존재하지 않는 `icd_code` | **400 `validation_error`** |
| 8 | 본문에 `patient_id` 위조 삽입 | **무시됨** — DB에는 예약의 실제 환자 |
| 9 | 차트 재조회 | 200 + 처방 항목 1건 |
| 10 | 환자 로그인 → `GET /api/bff/patient/prescriptions` | 200 + **본인 것만** |
| 11 | 다른 환자의 `visit_no` 조회 | **404** |

> 8번은 위조 방어 회귀 테스트다. §7.2-8번이 깨지면 여기서 잡힌다.

### 13.4 관측성 검증 — 두 흐름 공통

```powershell
# 오류를 하나 유발한 뒤 trace_id로 양쪽 로그를 대조한다
kubectl -n app logs deploy/bff --tail=200 | Select-String "<trace_id>"
kubectl -n app logs deploy/was --tail=200 | Select-String "<trace_id>"
```

- [x] 오류 응답 JSON에 `error` / `message` / `trace_id` 3필드
- [x] 응답 헤더에 `X-Trace-Id`
- [x] BFF·WAS 로그에서 동일 `trace_id`
- [x] **성공 응답에는 `trace_id` 없음**
- [x] 로그에 비밀번호·JWT·토큰 문자열 **없음**

CloudWatch 도입 이후에는 파드 소멸 후에도 조회 가능하다 (README §10.4):

```powershell
aws logs tail /aws/containerinsights/hybrid-toy-eks/application --follow --region ap-northeast-2
```

---

## §14. 배포 전 체크리스트

배포 직전에 이 목록을 한 번 훑는다. **전부 실패 이력이 있는 항목이다.**

### 14.1 코드

- [ ] `.\gradlew clean build` 통과 (WAS·BFF 각각)
- [ ] 주석에 `**/` 없음 — Javadoc 조기 종료
  ```powershell
  Select-String -Path apps -Include *.java -Pattern '\*\*/' -Recurse
  ```
- [ ] `application.yaml` 값이 ConfigMap 계약과 일치 (특히 **`JWT_AUDIENCE = hybrid-toy-api`**)
- [ ] `grep -ri "data.go.kr|apis.data" apps/was/src` → 0건

### 14.2 매니페스트

- [ ] 자리표시자 잔존 0건
  ```powershell
  Select-String -Path k8s -Include *.yaml -Pattern '__[A-Z_]+__' -Recurse
  ```
- [ ] **파드 limits × replicas ≤ ResourceQuota**
  ```powershell
  kubectl -n app describe resourcequota app-quota
  ```
- [ ] 이미지 태그가 `:latest` 아님 (cloudflared 포함)
- [ ] Ingress 경로 순서 — `/api/bff/patient` 먼저, `/` 마지막
- [ ] Ingress에 `/api/bff/staff` 규칙 **없음**
- [ ] ACM ARN 36자 UUID 온전

### 14.3 시크릿·파일

- [ ] `.\preflight.ps1` 전 항목 통과 (17개)
- [ ] 치환한 파일에 BOM 없음 (`Format-Hex` 첫 줄)
- [ ] `git status` 에 `.env` / `mysql-init/` / `*.local.sql` / `*.tfvars` 없음
- [ ] 커밋 완료 (미커밋이면 `deploy.ps1` 이 중단한다)

### 14.4 배포 후

- [ ] `kubectl -n app get pod` 전부 `Running` `1/1`
- [ ] 파드가 두 노드에 분산
- [ ] 배포 태그를 §18에 기록
- [ ] 검증 스크립트 실행
- [ ] (관측성 도입 후) CloudWatch에 새 파드가 표시되는지 확인

---

## §15. 완료 기준(DoD) 체크리스트

### 15.1 인프라 리드

- [x] `backend-bootstrap` apply, 버킷명 반영
- [x] apply 전 선조치 (§19.3의 코드 결함 4건 수정) 완료
- [x] `terraform apply` 에러 0
- [x] `terraform output` 전 항목 공유
- [x] 팀원 전원 `kubectl get nodes` 성공
- [x] ACM 인증서 **Issued**
- [x] LB Controller Pod Running (차트 `3.5.0` 고정)
- [x] `app` 네임스페이스 생성
- [x] `JWT_SIGNING_KEY` 발급 → BFF 담당 전달
- [x] Cloudflare SSL/TLS = **Full (strict)**
- [x] WAF / Rate Limit / Bot Fight 활성화
- [x] `www` CNAME 등록 (🟠 주황)
- [x] Tunnel 생성 + 토큰 BFF 담당 전달
- [x] Access App 생성 + Policy Action = **Service Auth**
- [x] **Protect with Access 활성화 + AUD 연결**
- [x] Service Token 발급 + OKD팀 전달
- [x] CloudWatch 애드온 설치 + Pod Identity 연결
- [x] 로그 그룹 보존기간 7일 적용

### 15.2 EKS팀 공통

- [x] `/healthz`, `/readyz` 200 (README §6.1 의미대로)
- [x] 이미지 git SHA 태그로 ECR 푸시 (amd64)
- [x] 베이스 이미지 digest 핀
- [x] 컨테이너 non-root 실행
- [x] Deployment + Service 배포, Pod Running
- [x] 하위 서비스 호출 성공
- [x] 매니페스트 `k8s/` 커밋 (실제 Secret 미포함)
- [x] URL 하드코딩 없음

### 15.3 EKS팀 담당별

- [x] **WAS**: RDS 커넥션 확인 / `sslMode=REQUIRED` / `JwtTokenProvider` 제거 완료 / X-Actor 2차 검증 동작 / `startupProbe` 적용
- [x] **BFF**: Nimbus JWT 발급·검증 / 전달수단 바인딩 4케이스 / 외부 `X-Actor-*` 제거 / cloudflared Running + Tunnel **HEALTHY**
- [x] **Web**: Ingress ADDRESS 표시 / Target Group **healthy** / `/api/bff/staff` 404 / charset 헤더

### 15.4 DB팀

- [x] DDL / GRANT / SEED **git 커밋 완료**
- [x] `commondb` 스키마 생성
- [x] `app_was` 계정 + 최소 권한 + `REQUIRE SSL`
- [x] WAS 담당에게 자격증명 전달
- [x] 직원 seed (`must_change_password = 0`) 투입
- [x] 예약 동시성 UNIQUE 제약 확인
- [x] `04_icd_seed.sql` 투입 — `icd_code` **14,283** / `icd_code_synonym` **37,543**
- [x] `chart.visit_no` UNIQUE / `prescription.chart_id` UNIQUE 확인
- [x] `app_was` 가 `icd_code`·`icd_code_synonym` 에 **SELECT 권한만** 보유

### 15.5 OKD팀

- [x] 아웃바운드 443 허용 확인
- [x] 공인 도메인 DNS 해석 확인
- [x] 프록시 / TLS 검사 대응 확인
- [x] Service Token을 OpenShift Secret으로 생성
- [ ] Web Pod → `200 application/json` 수신 — **범위 밖 이관**
- [ ] 직원 로그인 → Bearer JWT 획득 → 업무 API — **범위 밖 이관**
- [ ] 역할별 화면 분기 — **범위 밖 이관**

### 15.6 전체 흐름

**흐름1 (G2)**
- [x] 브라우저 → `www` → patient-web 화면 표시
- [x] `GET /auth/csrf` → `XSRF-TOKEN` 쿠키 수신
- [x] 환자 회원가입 201 / 로그인 → `PATIENT_TOKEN` 쿠키
- [x] 인증 후 환자 API 200 / 타 환자 데이터 404
- [x] 5회 실패 시 **423** / 로그아웃 시 쿠키 만료
- [x] 외부 도메인에서 `/api/bff/staff/...` → **404**
- [x] Cloudflare 대시보드에 트래픽 기록

**흐름2 (G3)**
- [x] Service Token 실패 **403**, 직원 JWT 실패 **401**, 역할 부족 **403**
- [x] 직원 로그인 → Bearer JWT (8시간)
- [x] DOCTOR / NURSE / ADMIN_STAFF 권한 분리 동작

**진료·처방 (G3)**
- [x] `GET /staff/patients` 당일 예약 목록 200
- [x] `GET /staff/icd-codes?q=당뇨` 50건 이하 / `?q=E` **400**
- [x] DOCTOR `POST /staff/charts` **201** / 재요청 **409** / NURSE **403**
- [x] 존재하지 않는 `icd_code` **400**
- [x] `GET /staff/patients/{visit_no}/chart` 200
- [x] 환자 `GET /patient/prescriptions` 본인 것만 / 타 환자 **404**
- [x] `chart.icd_name_snapshot` 발급 시점 명칭 저장

**내부 통신**
- [x] BFF가 외부 `X-Actor-*` 제거 / 검증한 JWT로 재생성
- [x] WAS가 `X-Actor-Id` 를 사용자 식별값으로 사용
- [x] `X-Trace-Id` 가 BFF·WAS 로그에서 일치

**관측성 (M1)**
- [x] CW 파드 5개 Running / `app` 파드 스펙 불변
- [x] 로그 그룹 4종 + 보존 7일
- [x] Container Insights 대시보드 표시

---

## §16. 장애 시 롤백

### 16.1 배포 롤백

```powershell
kubectl -n app rollout history deploy/<서비스>
kubectl -n app rollout undo deploy/<서비스>
kubectl -n app rollout status deploy/<서비스>
```

> 이미지가 `IMMUTABLE` + git SHA 태그이므로 **어느 커밋인지 즉시 역추적된다.**

### 16.2 절대 하지 말 것

| 금지 | 결과 |
|---|---|
| `kubectl delete -f k8s/` | Namespace 포함 시 **하위 전체 캐스케이드 삭제** |
| `terraform destroy` (팀원) | 단일 state 손상. 리드 전용 |
| S3 state 버킷 수동 삭제 | 버전 이력 = 유일한 포렌식 복구 수단 |
| RDS SG에 개인 IP 추가 | 격리 설계 무력화 |
| DB 스키마를 `app_was` 로 변경 | 권한이 없다. DB팀이 `admin` 으로 수행 |
| Ingress에 `/api/bff/staff` 추가 | 직원 API 외부 노출 |

### 16.3 DB 롤백

`skip_final_snapshot = true` 이므로 **RDS destroy = 데이터 완전 소실**이다. 복구 경로는 git의 DDL·seed 재실행뿐이다. 그래서 §6-4번(커밋)이 필수 단계다.

### 16.4 관측성 롤백

애드온이 문제를 일으키면 애플리케이션에 영향 없이 제거할 수 있다.

```powershell
aws eks delete-addon --cluster-name hybrid-toy-eks --addon-name amazon-cloudwatch-observability --region ap-northeast-2
```

파드 5개가 사라질 뿐 `app` 네임스페이스는 무관하다 — 이것이 Application Signals를 끈 실질적 이유다 (README §7.14).

---

## §17. 코드 산출 이력 및 배치 원칙

한 번에 전부 쏟아내면 검증 불가능한 덩어리가 된다. **배치로 끊어서 갔다.**

| 배치 | 산출물 | 검증 게이트 | 상태 |
|---|---|---|---|
| **1** | `db/01~03.sql` + `tools/gen-bcrypt.ps1` | 로컬 MySQL 8.0.46 실행 | ✅ |
| **1.5** | `tools/convert-icd.ps1` → `db/04_icd_seed.sql` | 14,283 / 37,543 | ✅ |
| **2a** | WAS 빌드·설정·엔티티 9·리포지토리 8 | 컴파일 | ✅ |
| **2b** | WAS 필터·오류·인증·업무 + `JpaBootstrapTest` | **W1** | ✅ |
| **2c** | `local/` compose + `verify-was.ps1` | **D3 · W2 (38/38)** | ✅ |
| **3a** | BFF `JwtIssuer`·`TokenResolver`·`SecurityConfig` | JWT 계약 5 | ✅ |
| **3b** | BFF 컨트롤러·`GlobalExceptionHandler` | 바인딩 계약 7 | ✅ |
| **3c** | compose 3단 + `verify-bff.ps1` | **B1 · B2 (45/45)** | ✅ |
| **4a** | `k8s/` 매니페스트 + `deploy.ps1` + `preflight.ps1` | 파드 기동 | ✅ |
| **4b** | `apps/patient-web/` + Ingress + `verify-flow1.ps1` | **G2 (24/24 + 브라우저)** | ✅ |
| **5** | cloudflared 배포 + `verify-flow2.ps1` (+ preflight 14→17) | **G3 (29/29)** | ✅ |
| **6** | `monitoring/` + CloudWatch 애드온 | **M1** | ✅ |

각 배치는 다음 형식으로 산출했다: ① 파일 트리 ② 파일 전문 ③ 로컬 검증 명령 ④ 통과 기준 ⑤ 다음 배치 진입 조건.

### 17.1 배치 진행에서 확인된 것

**① 검증 스크립트를 배치마다 함께 낸다.** 코드만 받고 검증을 나중에 만들면 "동작하는 것처럼 보이는 상태"가 누적된다. `JpaBootstrapTest` 가 없었다면 `@Query` JPQL 15개는 EKS 첫 기동에서야 검증됐을 것이다.

**② 실패는 배치 안에서 닫는다.** 다음 배치로 넘기면 원인이 섞인다. BFF는 컴파일 단계에서만 4번 막혔고(Boot 4 이관) 전부 그 자리에서 닫았다.

**③ 검증 통과가 동작을 보증하지 않는다.** G2에서 실증됐다. 하네스의 관대함을 계약으로 착각하지 않는다 (README §12.4).

### 17.2 반복된 실패 유형 — 실측 집계

| 유형 | 횟수 | 표준 대응 |
|---|---|---|
| PowerShell 5.1 파일 입출력 | **5회** | §3 (README §12.1) |
| Spring Boot 4 / Security 7 이관 | **4회** | README §11.3 |
| 식별자 입력 경로 (ARN·도메인 오염) | **3회** | §3-⑦ (README §12.2) |
| 매니페스트 총량 계산 (ResourceQuota) | **1회** | 진단이 가장 어려웠다 — §14.2 |

**③번이 진단 난이도 최고였다.** `limits.cpu: 1` × 4파드 = quota `4` 정확히 소진 → 파드가 생성되지 않아 `describe pod`·`describe rs`·로그·이벤트가 전부 비어 있었다. `describe resourcequota` 한 줄이 유일한 단서였다.

---

## §18. 배포 이력

> ECR immutable tag다. 태그는 git short SHA이며, **이 표가 이미지와 커밋을 잇는 유일한 추적 수단이다.**
> **배포할 때마다 여기에 기록한다.**

| 태그 | 대상 | 일자 | 비고 |
|---|---|---|---|
| `b631234` | WAS | 2026-08-12 | 최초 배포 |
| `b631234` | BFF | 2026-08-12 | 최초 배포 |
| `b07d408` | WAS · BFF · patient-web | 2026-08-12 | CPU limits 교정 (1 → 500m) |
| `7b35c0b` | BFF · patient-web | 2026-08-12 | CSRF 쿠키 Path 교정 |
| `cad77bb` | cloudflared | 2026-08-13 | digest 치환. ECR 미경유(퍼블릭 이미지 직접 배포) |

**애드온·차트 설치 이력**

| 대상 | 버전 | 일자 | 방식 |
|---|---|---|---|
| AWS Load Balancer Controller | `3.5.0` | 2026-08-10 | Helm (`--version` 고정) |
| Amazon CloudWatch Observability | `v6.5.0-eksbuild.1` | 2026-08-13 | EKS Addon (CLI) |

**인프라 생성 소요 시간 실측** (P1, 2026-08-10)

| 리소스 | 소요 |
|---|---|
| ECR ×3 + lifecycle ×3 | 1초 |
| VPC / 서브넷 / RT / IGW | 1~2초 |
| NAT Gateway | 1분 44초 |
| KMS Key | 21초 |
| EKS 클러스터 | 7분 43초 |
| 애드온 `before_compute` | 44초 |
| **노드그룹** | **1분 48초** |
| coredns / kube-proxy | 15초 / 25초 |
| RDS 인스턴스 | 6분 20초 |
| CloudWatch 애드온 (S7) | 45초 |

> **노드그룹 1분 48초** — `before_compute` 수정의 실물 검증. 이전 apply는 CNI 부재로 **33분 대기 후 실패**했다.

---

## §19. 프로젝트 종료 절차

### 19.1 종료 전 확인

- [ ] `db/` 의 DDL·seed가 git에 커밋되어 있는지 확인 (README §7.7 — 복구 경로가 이것뿐이다)
- [ ] `k8s/` 매니페스트 전량 커밋
- [ ] `monitoring/*.json` 커밋
- [ ] §18 배포 이력 최신화
- [ ] README §15 결정 로그 최신화
- [ ] 시연 스크린샷·로그에 시크릿 미포함 확인

### 19.2 Terraform 관리 밖 리소스 — 먼저 지운다

> 🚨 **`terraform destroy` 는 아래를 지우지 않는다.** 방치하면 과금이 남는다.

```powershell
# ① CloudWatch 애드온
aws eks delete-addon --cluster-name hybrid-toy-eks `
  --addon-name amazon-cloudwatch-observability --region ap-northeast-2

# ② IAM Role (정책 detach 후 삭제)
aws iam detach-role-policy --role-name hybrid-toy-eks-cwagent-role `
  --policy-arn arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
aws iam delete-role --role-name hybrid-toy-eks-cwagent-role

# ③ 로그 그룹 (보존 7일이라 방치해도 자동 소멸. 즉시 정리 시)
$groups = aws logs describe-log-groups `
  --log-group-name-prefix "/aws/containerinsights/hybrid-toy-eks" `
  --region ap-northeast-2 --query "logGroups[].logGroupName" --output text
foreach ($g in ($groups -split "\s+" | Where-Object { $_ })) {
  aws logs delete-log-group --log-group-name $g --region ap-northeast-2
}

# ④ Ingress 삭제 → ALB 회수 (destroy 전에 해야 VPC 삭제가 막히지 않는다)
kubectl -n app delete ingress patient-web
```

### 19.3 Cloudflare 정리 (대시보드 수동)

- [ ] Tunnel 삭제
- [ ] Access Application 삭제
- [ ] Service Token 폐기
- [ ] `www` / `staff-api` DNS 레코드 삭제

### 19.4 Terraform destroy

```powershell
cd infra/envs/dev
terraform plan -destroy -out=tfplan-destroy
terraform show -no-color tfplan-destroy > plan-destroy.txt
terraform apply tfplan-destroy
```

> ⚠️ **미실행 시 EKS $0.10/h + NAT + RDS가 계속 과금된다.**
> ⚠️ **S3 state 버킷은 `prevent_destroy` 로 남는다.** 의도된 동작이다 — 수동 삭제하지 않는다.

### 19.5 destroy 후 잔존 확인

```powershell
aws eks list-clusters --region ap-northeast-2
aws rds describe-db-instances --region ap-northeast-2 --query "DBInstances[].DBInstanceIdentifier"
aws ec2 describe-nat-gateways --region ap-northeast-2 --query "NatGateways[?State=='available'].NatGatewayId"
aws elbv2 describe-load-balancers --region ap-northeast-2 --query "LoadBalancers[].LoadBalancerName"
aws logs describe-log-groups --region ap-northeast-2 --query "logGroups[].logGroupName"
```

> **state가 비었어도 AWS 실물을 확인한다.** orphan 과금 + 재생성 시 `AlreadyExists` 의 원인이다.

---

## §20. 문서 개정 이력

### 20.1 구축설명서

| 버전 | 일자 | 변경 |
|---|---|---|
| v1.0 | 2026-08-08 | 최초 작성. 산출물 목록 · 역할 분담 · S0~S6 절차 |
| v1.1 | 2026-08-10 | 진료·처방 도메인 반영. ICD 변환기·seed 추가, DB팀 11~13단계 + D2 게이트, WAS 6.1b 신설, 배치 재편 |
| v1.2 | 2026-08-13 | 실행 절차 반영. §3.5 PowerShell 표준 신설, S1~S6을 실제 실행 결과로 교체, §9 EKS 배포 재작성, §14 배포 전 체크리스트 신설 |
| **v2.0** | **2026-08-13** | **최종 재편.** README v4.0 분리 원칙 반영 — 아래 |

**v1.2 → v2.0 변경**

| # | 변경 | 이유 |
|---|---|---|
| 1 | **§12 S7 관측성 구축 신설** | CloudWatch 도입 절차 6단계 + M1 게이트 |
| 2 | **README §14 DoD 체크리스트를 §15로 이관** | 체크리스트는 절차 문서의 것 |
| 3 | **README의 P0/P1/P2/P3 실행 기록·교훈을 흡수** | §17.2 반복 실패 집계, §18 소요 시간 실측 |
| 4 | **§18 배포 이력 신설** | README **v3.2** §22.5에서 이관 |
| 5 | **§19 프로젝트 종료 절차 신설** | state 밖 리소스 정리 포함 |
| 6 | **§20 문서 개정 이력 신설** | README의 버전 변경 요약 4개를 전부 흡수 |
| 7 | **모든 README 참조를 v4.0 번호로 갱신** | 절 번호 전면 재편 |
| 8 | §4 실행 순서 총괄 신설 | 마일스톤·게이트 정의를 한 곳에 집약 |
| 9 | 게이트 통과 일자를 부록 A로 집약 | — |

### 20.2 README (가이드라인)

| 버전 | 일자 | 변경 |
|---|---|---|
| v1.0 | 2026-08-07 | 최초 작성 |
| v2.1 | 2026-08-08 | 애플리케이션 계약 v2 별도 문서 운영 |
| v3.0 | 2026-08-10 | **애플리케이션 계약을 README에 흡수** (§6.5~§6.9 신설). Ingress 2-path 교체, cloudflared digest 핀, JDBC `sslMode`, WAS JWT 발급 전면 금지, 앱 스택 버전 전량 핀, 확정 결정 로그 신설 |
| v3.1 | 2026-08-10 | 진료·처방 도메인 1건. 테이블 5종 확정, 공공데이터 적재 계약 신설, WAS 런타임 외부 호출 금지, 처방 발급 단일 트랜잭션 확정 |
| v3.2 | 2026-08-13 | P2·P3 실행 기록 반영. CSRF 쿠키 `Path=/` 교정, `JWT_AUDIENCE` 재확인, 파드 리소스 계약 확정, 트러블슈팅 20건 추가, 결정 로그 #32~#44 |
| **v4.0** | **2026-08-13** | **최종 재편.** 절 번호 전면 개편(부록 A 매핑), 관측성 §10 신설, 설계 판단 §7.13·§7.14 추가, 반복 실패 패턴 §12 신설, 트러블슈팅 6개 카테고리 재분류, 결정 로그 #45~#51 추가, **실행 절차·체크리스트·기록·날짜를 구축설명서로 이관** |

**v3.2 → v4.0에서 이 문서로 이관된 것**

| README v3.2 절 | 이관 위치 |
|---|---|
| 버전 변경 요약 표 4개 | §20.2 |
| §8 apply 전 필수 선조치 | §5 / §17.2 |
| §9 전체 실행 순서 및 게이트 | §4 |
| §10 인프라 리드팀 실행 절차 | §5 / §10.4 / §11.1 |
| §11 EKS팀 실행 절차 | §7 / §8 / §9 |
| §12 DB팀 실행 절차 | §6 |
| §14 완료 기준(DoD) 체크리스트 | §15 |
| §19 P0 기록 및 교훈 | §17.2 / §19.5 |
| §20 P1 기록 및 교훈 | §18 (소요 시간) / §5 (온보딩) |
| §22 P2·P3 실행 기록 | §17 / §18 / §10.5 / §11.6 |

---

## 부록 A. 게이트 통과 기록

| 게이트 | 조건 | 결과 | 통과일 |
|---|---|---|---|
| **G1** | `terraform output` 공유 + 전원 `kubectl get nodes` | ✅ | 2026-08-10 |
| **S0** | 리드 선행 6항목 + 환경 점검 4인 | ✅ | 2026-08-10 |
| **D1** | `app_was` `SELECT 1` + `Ssl_cipher` 비어있지 않음 | ✅ | 2026-08-12 |
| **D2** | `icd_code` 14,283 / `icd_code_synonym` 37,543 | ✅ | 2026-08-12 |
| **W1** | WAS 컨텍스트 기동 + JPQL 전량 파싱 | ✅ | 2026-08-12 |
| **D3** | WAS `/readyz` → `db:up` (`ddl-auto: validate`) | ✅ | 2026-08-12 |
| **W2** | `verify-was.ps1` **38/38** | ✅ | 2026-08-12 |
| **B1** | BFF `/readyz` → `{"status":"ok"}` | ✅ | 2026-08-12 |
| **B2** | `verify-bff.ps1` **45/45** | ✅ | 2026-08-12 |
| **G2** | `verify-flow1.ps1` **24/24** + 브라우저 10항목 | ✅ | 2026-08-12 |
| **G3** | `verify-flow2.ps1` **29/29** | ✅ | 2026-08-13 |
| **M1** | CW 파드 5개 + 파드 스펙 불변 + 로그 보존 7일 | ✅ | 2026-08-13 |

**범위 밖 이관**

| 항목 | 사유 |
|---|---|
| OKD Web 애플리케이션 구현 (§11.4의 3~7번) | 프로젝트 기간 제약 |
| CloudWatch Alarm | 잔여 시간 부족. README §16.3 백로그 |
| README §16.3 기능 백로그 전체 | 뼈대 완성 우선 원칙 (README §0.3) |

---

## 재개 시 시작점

1. **README §15 확정 결정 로그** — 무엇이 이미 결정됐는지
2. **README §6 표준 및 계약** — 코드가 지켜야 할 것
3. **이 문서 §17 코드 산출 이력** — 어디까지 만들었는지
4. **README §16 백로그** — 다음에 할 것

계약(README §6)은 G3 이후 변경되지 않았으므로 그대로 유효하다.
