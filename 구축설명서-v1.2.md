# Hybrid Toy Project — 구축설명서 v1.2

> **상위 문서: `README.md` v3.2.** 충돌 시 README가 이긴다.
> 이 문서는 **"무엇을 누가 만들고, 어떤 순서로 실행하는가"** 만 다룬다.
> 설계 근거·계약 스펙은 README에 있다. 여기서 반복하지 않는다.

| 항목 | 값 |
|---|---|
| 문서 버전 | **v1.2** |
| 기준일 | 2026-08-13 |
| 전제 | 🚩 **G3 통과 — 뼈대(G1~G3) 완성.** OKD Web 은 범위 밖 이관 |
| 목표 | — (프로젝트 종료. 재개 시 README §17.3 백로그 참조) |
| 대상 | 인프라 리드 / WAS / BFF / Web / DB / OKD |

### v1.1 → v1.2 변경 요약

> **실행 절차 반영.** 계약이 아니라 "실제로 어떻게 굴렸는가" 의 기록이다.

| # | 변경 | 대상 |
|---|---|---|
| 1 | §1 산출물에 `apps/bff`, `apps/patient-web`, `k8s/`, `scripts/` 실제 구조 반영 | §1 |
| 2 | **§3.5 신설 — PowerShell 표준 절차** (인코딩·실행정책·파일 치환) | §3 |
| 3 | §5~§8 각 팀 절차를 **실제 실행 결과**로 교체 | §5~§8 |
| 4 | **§9 재작성 — EKS 배포 절차** (`preflight` → `deploy` → Ingress) | §9 |
| 5 | §11 검증 시나리오 건수 갱신 (38 / 45 / 24) | §11 |
| 6 | §13 산출 배치를 실제 이력(1 ~ 4b)으로 교체 | §13 |
| 7 | **§14 신설 — 배포 전 체크리스트** | §14 |

### v1.0 → v1.1 변경 요약

> **진료·처방 도메인 반영 1건.** 인증·네트워크·배포 절차는 무변경이다.

| # | 변경 | 대상 |
|---|---|---|
| 1 | `db/04_icd_seed.sql` · `db/tools/convert-icd.ps1` · `db/raw/data.json` 추가 | §1, §2.1 |
| 2 | §5 DB팀에 **11~13번 단계 + 게이트 D2** 추가 | §5 |
| 3 | §6 WAS에 **6.1b 진료·처방 도메인** 신설 | §6 |
| 4 | §11 통합 검증에 **11.4 진료·처방 시나리오** 신설 | §11 |
| 5 | §13 산출 배치를 **1 → 1.5 → 2 …** 로 재편 (ICD 변환기 선행) | §13 |
| 6 | §3.4 "ICD 코드 seed 범위" 결정 → **확정 처리** (README §21-28) | §3.4 |

> **BFF·Web·OKD·리드 담당의 절차는 한 줄도 바뀌지 않았다.** §7·§8·§9·§10을 다시 읽을 필요 없다.

---

## 목차

- [§0. 이 문서를 읽는 순서](#0-이-문서를-읽는-순서)
- [§1. 산출물 전체 목록](#1-산출물-전체-목록)
- [§2. AI가 생성할 수 있는 부분](#2-ai가-생성할-수-있는-부분)
- [§3. 사람이 직접 해야 하는 부분](#3-사람이-직접-해야-하는-부분)
- [§4. S0 — 착수 전 리드 선행 작업](#4-s0--착수-전-리드-선행-작업)
- [§5. S1 — DB팀](#5-s1--db팀)
- [§6. S2 — WAS 담당](#6-s2--was-담당)
- [§7. S3 — BFF 담당](#7-s3--bff-담당)
- [§8. S4 — Web 담당](#8-s4--web-담당)
- [§9. S5 — 인프라 리드 → G2](#9-s5--인프라-리드--g2)
- [§10. S6 — 내부 흐름 → G3](#10-s6--내부-흐름--g3)
- [§11. 통합 검증 시나리오](#11-통합-검증-시나리오)
- [§12. 장애 시 롤백](#12-장애-시-롤백)
- [§13. 코드 산출 순서 제안](#13-코드-산출-순서-제안)

---

## §0. 이 문서를 읽는 순서

| 역할 | 필독 |
|---|---|
| 인프라 리드 | 전체 |
| DB 담당 (`kusdb`) | §1, §3, §4, §5, §11 |
| WAS 담당 (`kuswas`) | §1, §2, §6, §11 |
| BFF 담당 (`kusbff`) | §1, §2, §7, §10, §11 |
| Web 담당 (`kusweb`) | §1, §2, §8, §11 |
| OKD 담당 | §3, §10, §11 + README §18 |

**전 팀 공통 3원칙**

1. **계약이 코드보다 위다.** README §6.5~§6.9와 다르게 구현했으면 코드가 틀린 것이다.
2. **각 단계는 게이트를 통과해야 다음으로 간다.** 검증 명령이 없는 "다 됐어요"는 완료가 아니다.
3. **식별자는 손으로 옮기지 않는다.** ARN·도메인·토큰은 `output -raw` → 변수 → 명령 인자.

---

## §1. 산출물 전체 목록

```
repo/
├── README.md                                   [리드]    ✅ v3.1 완료
├── 구축설명서-v1.2.md                           [리드]    ✅ 이 문서
│
├── db/                                          [DB팀]
│   ├── 01_schema.sql                                     계정·업무·진료·처방 테이블 DDL
│   ├── 02_grants.sql                                     app_was 최소 권한 + REQUIRE SSL + ICD REVOKE
│   ├── 03_seed.sql                                       직원·의사·진료슬롯 seed
│   ├── 04_icd_seed.sql                          ★v1.1   KCD 상병코드 (14,283 + 37,543행)
│   ├── raw/data.json                            ★v1.1   공공데이터 원본 19MB / 47,798행
│   └── tools/
│       ├── gen-bcrypt.ps1                                BCrypt 해시 생성기
│       └── convert-icd.ps1                      ★v1.1   data.json → 04_icd_seed.sql
│
├── apps/was/                                    [WAS]
│   ├── Dockerfile                                        멀티스테이지 / non-root / amd64
│   ├── .dockerignore
│   ├── build.gradle.kts, settings.gradle.kts, gradle/wrapper/
│   └── src/main/
│       ├── resources/application.yaml                    SNAKE_CASE / sslMode / TZ
│       └── java/.../
│           ├── config/           SecurityConfig(내부용), PasswordEncoderConfig, JacksonConfig
│           ├── web/filter/       ActorHeaderFilter(2차 검증), TraceIdFilter
│           ├── auth/             PatientAuthController/Service, StaffAuthController/Service
│           ├── domain/           PatientUser, StaffUser, Doctor, Appointment,
│           │                     Chart, Prescription, PrescriptionItem,     ★v1.1
│           │                     IcdCode, IcdCodeSynonym                    ★v1.1
│           ├── repository/       JPA Repository (+ IcdCodeSynonymRepository ★v1.1)
│           ├── biz/              PatientApiController, StaffApiController,
│           │                     ChartService(차트+처방 단일 트랜잭션)      ★v1.1
│           ├── error/            ApiError, GlobalExceptionHandler, ErrorCode
│           └── health/           HealthController(/healthz, /readyz-DB)
│
├── apps/bff/                                    [BFF]
│   ├── Dockerfile, .dockerignore
│   ├── build.gradle.kts, settings.gradle.kts, gradle/wrapper/
│   └── src/main/
│       ├── resources/application.yaml
│       └── java/.../
│           ├── config/           SecurityConfig, JwtConfig(Nimbus), RestClientConfig
│           ├── security/         TokenResolver, ActorBindingValidator, JwtIssuer
│           ├── filter/           TraceIdFilter, InboundActorHeaderStripFilter
│           ├── patient/          PatientAuthController, PatientApiController
│           ├── staff/            StaffAuthController, StaffApiController
│           ├── client/           WasClient (timeout 2s/5s, retry 없음, 503 변환)
│           ├── error/            ApiError, GlobalExceptionHandler
│           └── health/           HealthController(/healthz, /readyz)
│
├── apps/patient-web/                            [Web]
│   ├── Dockerfile                                        nginx-unprivileged:stable-alpine
│   ├── nginx.conf                                        8080 / SPA fallback / staff 404 / healthz
│   └── (기존 SPA 소스 — 구현 완료 보고됨)
│
├── local/
│   ├── docker-compose.yml                       [EKS팀]  web→bff→was→mysql
│   └── mysql-init/                                       01~04 sql 심볼릭 참조  ★v1.1
│
├── k8s/                                         [EKS팀]
│   ├── namespace.yaml
│   ├── was/{configmap,secret.example,deployment,service}.yaml
│   ├── bff/{configmap,secret.example,deployment,service}.yaml
│   ├── patient-web/{configmap,deployment,service}.yaml
│   ├── cloudflared/{secret.example,deployment}.yaml
│   └── ingress/patient-web-ingress.yaml                  ★ 2-path 확정본
│
└── scripts/
    ├── build-push.ps1                           [EKS팀]  git SHA 태그 빌드·푸시
    ├── pin-base-images.ps1                      [EKS팀]  베이스 이미지 digest 조회
    └── verify-flow1.ps1 / verify-flow2.ps1      [리드]   G2/G3 검증 스크립트
```

---

## §2. AI가 생성할 수 있는 부분

### 2.1 생성 가능 — 계약에서 기계적으로 유도되는 산출물

| # | 산출물 | 근거 계약 | 비고 |
|---|---|---|---|
| 1 | `db/01_schema.sql` | README §6.8 | 계정 + 업무 테이블. 예약 UNIQUE 제약 포함 |
| 2 | `db/02_grants.sql` | README §12.3 | `app_was` + `REQUIRE SSL` |
| 3 | `db/03_seed.sql` | README §6.8 | 직원 3역할 + 의사 + ICD 코드. `must_change_password=0` |
| 4 | `db/tools/gen-bcrypt.ps1` | — | 해시 생성. **비밀번호 값은 사람이 입력** |
| 5 | `apps/was/**` 전체 | §6.5~§6.8 | 인증 3종 + 업무 API + X-Actor 2차 검증 + 잠금 + 헬스 |
| 6 | `apps/bff/**` 전체 | §6.5~§6.7 | Nimbus JWT + CSRF + 쿠키 + 바인딩 + trace_id + 503 변환 |
| 7 | `apps/*/Dockerfile` | §5.3, §6.1 | 멀티스테이지 / non-root / digest 핀 자리 |
| 8 | `apps/patient-web/nginx.conf` + Dockerfile | §6.9 | 8080 / staff 404 / SPA fallback / `/healthz` |
| 9 | `local/docker-compose.yml` | §11.1 | 클러스터 없이 전 구간 통합 검증 |
| 10 | `k8s/**` 매니페스트 전량 | §6.1~§6.3, §6.9 | Ingress 2-path 확정본 포함 |
| 11 | `scripts/*.ps1` | §11.2 | PowerShell. `jq` 미사용 |
| 12 | `verify-flow1/2.ps1` | §14.5 | DoD 체크리스트를 실행 가능한 형태로 |
| **13** | `db/tools/convert-icd.ps1` | §6.10 | `data.json` → SQL. A안 필터 + 마스터/동의어 분리 + 1,000행 배치 |
| **14** | `db/04_icd_seed.sql` | §6.10 | 13번의 산출물. **14,283 + 37,543행** |
| **15** | `01_schema.sql` 진료·처방 DDL | §6.8 | `icd_code`·`icd_code_synonym`·`chart`·`prescription`·`prescription_item` + 인덱스 3종 |
| **16** | WAS 진료·처방 도메인 | §6.6, §6.8 | `ChartService` 단일 트랜잭션 / ICD 검색 / snapshot 저장 |
| **17** | `verify-clinical.ps1` | §11.4 | 처방 발급 11개 시나리오 |

> ✅ **v1.1** — 13~17번은 전부 계약에서 기계적으로 유도된다. 새 판단이 필요한 부분은 없다. A안 필터·테이블 구조·API 형태는 README §21-27~31로 이미 확정되었다.

### 2.2 생성 불가 — 구조적 이유

| 산출물 | 왜 불가 |
|---|---|
| `terraform.tfvars` 실값 | 커밋 금지 대상. 리드 로컬에만 존재 |
| 실제 Secret 매니페스트 | 값 자체가 시크릿 |
| 베이스 이미지 digest | 조회 시점에만 확정. **명령은 제공, 값은 사람이 채움** |
| Cloudflare 대시보드 설정 | UI 조작. API 토큰 미보유 |
| OKD Web 애플리케이션 | OKD팀 소관. **샘플 프록시 코드는 제공 가능** |
| 실제 빌드·푸시·apply 실행 | 로컬 Docker·AWS 자격증명 필요 |
| **공공데이터포털 서비스키** | 이번 단계에서는 **불필요하다.** 오프라인 seed 채택(README §7.11). 백로그의 자동 동기화 도입 시에만 발급 |
| **`data.json` 원본 다운로드** | 포털 로그인·약관 동의가 필요. **파일은 이미 확보되어 있다** — `db/raw/` 에 커밋만 하면 된다 |

### 2.3 리스크 고지 — 숨기지 않는다

| 리스크 | 내용 | 대응 |
|---|---|---|
| **최신 스택** | Spring Boot 4.1.0(2026-06 GA) + Spring Framework 7 + Spring Security 7 + Jackson 3. API 표면이 매우 새롭다 | **첫 `gradle build`에서 컴파일 에러 가능성을 전제**한다. 에러 로그를 주면 즉시 교정 |
| **Jackson 3 마이그레이션** | 패키지·설정 프로퍼티가 Jackson 2와 다르다 | `spring.jackson.property-naming-strategy` 동작을 통합테스트로 확인 |
| **Spring Security 7** | Boot 4.0에서 deprecated된 API가 4.1에서 **제거**됨 | lambda DSL만 사용. 구식 예제 복사 금지 |
| **Nimbus 직접 사용 금지** | 계약상 `JwtEncoder`/`JwtDecoder` 중심 | 저수준 Nimbus API 확장 지양 |
| **기존 코드와의 충돌** | WAS에 `JwtTokenProvider` 잔존, DB에 `must_change_password DEFAULT 1` | 생성물은 **레퍼런스 구현**이다. 기존 코드를 여기로 수렴시킨다 |

> 이 프로젝트의 실제 상태는 "백지"가 아니다. **부분 구현 + 계약 불일치**다.
> Web은 구현 완료, DB는 스키마 존재, WAS는 구 설계(JWT 발급) 잔존, BFF는 미배포.
> 따라서 작업의 본질은 **"새로 짓기"가 아니라 "계약으로 수렴시키기"** 다.

---

## §3. 사람이 직접 해야 하는 부분

### 3.1 시크릿 — 절대 위임 불가

| # | 항목 | 주체 | 생성 방법 | 전달 대상 |
|---|---|---|---|---|
| 1 | `JWT_SIGNING_KEY` | 리드 | 아래 명령 | BFF 담당 |
| 2 | RDS 마스터 비밀번호 조회 | 리드/DB | Secrets Manager | DB팀 (계정 생성 전용) |
| 3 | `app_was` 비밀번호 | DB팀 | 임의 강력 문자열 | WAS 담당 |
| 4 | 직원 seed 평문 비밀번호 | DB팀 | 팀 내부 합의 | 테스트용, 문서화 금지 |
| 5 | Cloudflare Tunnel Token | 리드 | 대시보드 | BFF 담당 |
| 6 | Service Token ID / Secret | 리드 | 대시보드 (**1회만 표시**) | OKD팀 |

```powershell
# JWT_SIGNING_KEY — 48바이트 랜덤 Base64
$b = New-Object byte[] 48
[System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b)
[Convert]::ToBase64String($b)
```

```powershell
# RDS 마스터 자격 (계정 생성에만 사용)
aws secretsmanager get-secret-value --secret-id "arn:aws:secretsmanager:ap-northeast-2:597106152264:secret:rds!db-83b090eb-78f5-4c86-8c9d-5f242c6b4cc9-vPq6SX" --query SecretString --output text
```

> 🚨 위 6개는 **채팅·이슈·PR·README·스크린샷 어디에도 남기지 않는다.**

### 3.2 Cloudflare 대시보드 — 전량 수동

| # | 작업 | 위치 | 실패 시 증상 |
|---|---|---|---|
| 1 | SSL/TLS = **Full (strict)** | SSL/TLS → Overview | 무한 리다이렉트 루프 |
| 2 | `www` CNAME → ALB DNS (**🟠 주황**) | DNS | 회색이면 WAF·DDoS 무력화 |
| 3 | WAF Managed Ruleset 활성화 | Security → WAF | — |
| 4 | Rate Limiting Rule | Security | — |
| 5 | Bot Fight Mode | Security → Bots | — |
| 6 | Tunnel 생성 + Token 발급 | Zero Trust → Networks → Tunnels | — |
| 7 | Public Hostname 등록 | Tunnel 설정 | `staff-api` DNS 자동 생성 |
| 8 | Access Application 생성 | Zero Trust → Access | — |
| 9 | Policy **Action = `Service Auth`** | Access → Policies | `Allow`면 OKD가 HTML 수신 |
| 10 | **Protect with Access 활성화 + AUD 선택** | Tunnel 설정 | 미설정 시 Access 우회 가능 |
| 11 | Service Token 발급 | Zero Trust → Access → Service Auth | — |

> 🚨 **9번과 10번이 P3 최다 실패 지점이다.** 둘 다 확인하기 전에는 OKD팀에 착수 지시를 내리지 않는다.
> 🚨 **`staff-api` DNS 레코드를 미리 만들지 않는다.** Tunnel이 자동 생성하며 수동 레코드가 있으면 충돌한다.

### 3.3 로컬 환경 · 실행 행위

| # | 작업 | 주체 |
|---|---|---|
| 1 | Docker Desktop 설치 및 기동 | EKS팀 3인 |
| 2 | JDK 25 설치 (Corretto 권장) | WAS·BFF |
| 3 | 베이스 이미지 **digest 조회 → 기록 → 커밋** | EKS팀 3인 |
| 4 | 이미지 빌드·푸시 (ECR 로그인 포함) | EKS팀 3인 |
| 5 | `kubectl apply` 실행 | 각 담당 |
| 6 | `kubectl create secret` 실행 | 각 담당 |
| 7 | 임시 파드로 RDS 접속 및 SQL 실행 | DB팀 |
| 8 | `terraform apply` (필요 시) | **리드 전용** |

```powershell
# 베이스 이미지 digest 조회 — 3개 전부 수행 후 README §21 표에 기록
foreach ($img in @("amazoncorretto:25-alpine","nginxinc/nginx-unprivileged:stable-alpine","cloudflare/cloudflared:latest")) {
  docker pull $img | Out-Null
  docker inspect --format="{{index .RepoDigests 0}}" $img
}
```

### 3.4 판단이 필요한 결정

| # | 항목 | 주체 |
|---|---|---|
| 1 | 직원 테스트 계정 수·역할 구성 | DB + OKD 협의 |
| 2 | 진료 슬롯 시간 단위 (30분 등) | DB + WAS 협의 |
| 3 | ~~ICD 코드 seed 범위~~ | ✅ **확정 완료** — A안. README §21-28 |
| 4 | Rate Limit 임계값 | 리드 |
| 5 | 데모 시나리오 (G2/G3 시연 순서) | 전 팀 |

---

### 3.5 PowerShell 표준 절차 — ✅ v1.2 신설

> P2 에서 **PowerShell 5.1 파일 입출력 문제로만 5회** 막혔다.
> 아래를 지키면 전부 예방된다.

**① 세션 시작 3줄** — 새 창을 열 때마다 필요하다

```powershell
[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding $false
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
Get-ChildItem -Recurse -Include *.ps1 | Unblock-File
```

zip 으로 받은 `.ps1` 은 인터넷 표식(MOTW)이 붙어 있어 `Unblock-File` 이 필요하다.
매번 치기 싫으면 사용자 범위로 한 번만 설정한다 (관리자 권한 불필요).

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
Add-Content $PROFILE '[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding $false'
```

**② 파일 치환은 이 함수만 쓴다**

```powershell
function Set-Placeholder([string]$Path, [string]$From, [string]$To) {
    # .NET 은 PowerShell 의 현재 위치를 모른다. 절대 경로로 변환한다.
    $full = (Resolve-Path $Path).Path
    $enc  = New-Object System.Text.UTF8Encoding $false      # BOM 없음
    $text = [System.IO.File]::ReadAllText($full, [System.Text.Encoding]::UTF8)
    if ($text -notlike "*$From*") { Write-Host "  [SKIP] $Path" -ForegroundColor Yellow; return }
    [System.IO.File]::WriteAllText($full, $text.Replace($From, $To), $enc)
    Write-Host "  [OK] $Path" -ForegroundColor Green
}
```

⚠️ **`Set-Content -Encoding UTF8` 을 쓰지 않는다.** PS 5.1 은 BOM 을 붙이고
원본을 CP949 로 읽는다. YAML 의 BOM 은 첫 키를, Dockerfile 의 BOM 은 `FROM` 을 깨뜨린다.

**③ 치환 후 반드시 확인한다**

```powershell
$b = [System.IO.File]::ReadAllBytes((Resolve-Path $p).Path)
"BOM = $($b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF)"      # False 여야 한다
[System.IO.File]::ReadAllText((Resolve-Path $p).Path, [System.Text.Encoding]::UTF8) | Select-String '검색어'
```

`Get-Content -Encoding UTF8` 도 5.1 에서는 신뢰할 수 없다. 확인은 `ReadAllText` 로 한다.

**④ 생성하는 `.ps1` 은 UTF-8 BOM + CRLF 로 저장한다**

PS 5.1 은 BOM 없는 `.ps1` 을 CP949 로 파싱해 한글 주석에서 구문 오류를 낸다.

**⑤ 값을 명령행에 직접 넣지 않는다**

```powershell
# 나쁨 — 히스토리에 남고, 따옴표가 벗겨질 수 있다
aws secretsmanager put-secret-value --secret-string '{"K":"value"}'

# 좋음 — 파일 경유 후 삭제
$tmp = Join-Path $env:TEMP 's.json'
[System.IO.File]::WriteAllText($tmp, ($obj | ConvertTo-Json -Compress), (New-Object System.Text.UTF8Encoding $false))
aws secretsmanager put-secret-value --secret-string "file://$tmp"
Remove-Item $tmp -Force
```

**⑥ `$ErrorActionPreference = 'Stop'` 을 네이티브 명령 스크립트에 걸지 않는다**

`aws`·`docker`·`kubectl` 은 정상 동작 중에도 stderr 에 쓴다. `'Stop'` 이면
그것만으로 스크립트가 죽는다. `'Continue'` 로 두고 판정을 **전부 `$LASTEXITCODE`** 로 한다.

---

## §4. S0 — 착수 전 리드 선행 작업

> **소요 ~30분. 이게 끝나기 전에는 어느 팀도 배포를 시작하지 않는다.**

| # | 작업 | 검증 |
|---|---|---|
| 1 | README v3.0 + 이 문서 merge, 전 팀 공유 | PR merged |
| 2 | 구 문서(`애플리케이션 통합 최종 채택안 v2`, DEV-GUIDELINE 등)에 **"README v3.0으로 대체됨"** 표기 | — |
| 3 | `JWT_SIGNING_KEY` 발급 → BFF 담당 전달 | BFF가 수신 확인 |
| 4 | G1 공유 항목 재공지 (README §20.1) | — |
| 5 | 팀별 담당 디렉토리·브랜치 규칙 확인 | README §3.1 |
| 6 | 레퍼런스 코드 세트 배포 (`feature/app-baseline`) | — |

**전 팀 환경 점검 (각자 1회)**

```powershell
aws sts get-caller-identity                                              # 본인 계정 확인
aws eks update-kubeconfig --region ap-northeast-2 --name hybrid-toy-eks
kubectl get nodes                                                        # Ready 2개
kubectl -n app get all                                                   # app 네임스페이스 접근 확인
docker version                                                           # Docker 기동 확인
```

> ※ 명령을 여러 줄로 나누어 붙여넣지 말 것 (README §20.8).

**🔒 게이트 S0**: 6개 항목 완료 + 팀원 4명 전원 환경 점검 통과.

---

## §5. S1 — DB팀

> **임계 경로의 시작점.** WAS가 이것을 기다린다. 소요 1~2시간.

| # | 작업 | 명령 / 검증 |
|---|---|---|
| 1 | 직원 계정 BCrypt 해시 생성 | `./db/tools/gen-bcrypt.ps1` → `$2b$`로 시작하는 문자열 확인 |
| 2 | `01_schema.sql` 로컬 MySQL 8.0에서 선검증 | 에러 0 |
| 3 | `03_seed.sql`에 해시 반영 (**평문 금지**) | grep으로 평문 없음 확인 |
| 4 | **git 커밋** — DDL/GRANT/SEED 3종 | `git ls-files db/` |
| 5 | 임시 파드로 RDS 접속 | 아래 |
| 6 | `commondb` 생성 → `01_schema.sql` 실행 | `SHOW TABLES;` |
| 7 | `02_grants.sql` 실행 | `SHOW GRANTS FOR 'app_was'@'%';` |
| 8 | `03_seed.sql` 실행 | `SELECT login_id, role, status, must_change_password FROM staff_user;` |
| 9 | `app_was` 계정으로 재접속 검증 | 아래 |
| 10 | 자격증명 → WAS 담당 전달 | **🔒 게이트 D1 — 여기서 WAS 담당이 착수한다** |
| **11** | `convert-icd.ps1` 실행 → `04_icd_seed.sql` 생성 | 아래 |
| **12** | `04_icd_seed.sql` **git 커밋** | `git ls-files db/` |
| **13** | `04_icd_seed.sql` 실행 → 건수 검증 | **🔒 게이트 D2** |

**5번 — 임시 파드 접속**

```powershell
kubectl -n app run mysql-cli --rm -it --image=mysql:8.0 --restart=Never -- mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u admin -p
```

**9번 — `app_was` 로 TLS 접속 검증** (이게 통과해야 WAS가 뜬다)

```powershell
kubectl -n app run mysql-cli --rm -it --image=mysql:8.0 --restart=Never -- mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u app_was -p -e "SELECT 1; SHOW STATUS LIKE 'Ssl_cipher';"
```

> `Ssl_cipher`가 **비어 있지 않아야** 한다. 비어 있으면 TLS 미적용 상태다.

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| `Access denied` | `REQUIRE SSL` 걸린 계정으로 평문 접속 시도 |
| 파드가 `Pending` | 노드 리소스. `kubectl -n app describe pod mysql-cli` |
| 접속 timeout | 파드가 `app` 네임스페이스가 아님 / RDS SG |
| `CREATE USER` 권한 오류 | `admin`이 아닌 계정으로 실행 |

**🔒 게이트 D1**: WAS 담당이 `app_was` 자격으로 `SELECT 1` 성공 + `Ssl_cipher` 비어 있지 않음.

---

### 5.1 ICD 상병코드 적재 — ✅ v1.1 신설

> **D1 이후 작업이다. WAS를 막지 않는다.** WAS 담당은 D1만 받고 즉시 착수한다.
> 계약은 README §6.10. 여기서는 실행만 다룬다.

**11번 — SQL 생성** (로컬 PowerShell, RDS 접속 불필요)

> ✅ **`04_icd_seed.sql` 은 이미 생성·검증되어 저장소에 있다. 이 단계는 건너뛰어도 된다.**
> KCD 개정본으로 다시 만들 때만 실행한다.

```powershell
cd db
.\tools\convert-icd.ps1 -InputPath .\raw\data.json -OutputPath .\04_icd_seed.sql
```

> ⚠️ PowerShell 5.1 은 19MB JSON 파싱에 수십 초가 걸리고, 환경에 따라
> `maxJsonLength` 제한에 걸린다. 스크립트에 폴백이 들어 있으나 **`pwsh`(PS7) 사용을 권한다.**

기대 출력:

```
[READ ] 47798 rows
[FILTER] excluded 10255  (완전코드구분='N' or 주상병사용구분='N')
[MASTER] icd_code          14283
[SYNONYM] icd_code_synonym 37543
[WRITE] 04_icd_seed.sql  (UTF-8 no BOM)
```

> ⚠️ **`[MASTER] 0` 이 나오면 필터가 반전된 것이다.** 원본에 `'Y'` 값은 존재하지 않는다. `<> 'N'` 이 맞다 (README §6.10).

**13번 — 적재 및 검증**

파일이 크므로 대화형 셸에 붙여넣지 않는다. **`source` 로 읽힌다.**

```powershell
# 파일을 파드로 전달 → 실행
kubectl -n app run mysql-cli --image=mysql:8.0 --restart=Never --command -- sleep 3600
kubectl -n app cp .\db\04_icd_seed.sql mysql-cli:/tmp/04_icd_seed.sql
kubectl -n app exec -it mysql-cli -- bash -lc "mysql -h hybrid-toy-rds.cfmws2co6i6j.ap-northeast-2.rds.amazonaws.com -u admin -p commondb < /tmp/04_icd_seed.sql"
kubectl -n app delete pod mysql-cli
```

**검증 SQL** — 한 번에 실행

```sql
SELECT (SELECT COUNT(*) FROM icd_code)                          AS icd_code,      -- 14283
       (SELECT COUNT(*) FROM icd_code_synonym)                  AS synonym,       -- 37543
       (SELECT COUNT(*) FROM icd_code_synonym WHERE code='E1140') AS e1140,       -- 60
       (SELECT COUNT(*) FROM icd_code_synonym s
          LEFT JOIN icd_code m ON m.code = s.code
         WHERE m.code IS NULL)                                  AS orphan;        -- 0

SELECT name_kr FROM icd_code WHERE code = 'E1140';
-- 기대: 당뇨병성 단일신경병증을 동반한 2형 당뇨병   (한글이 ??? 이면 인코딩 문제)

SELECT table_name, table_rows FROM information_schema.tables
 WHERE table_schema='commondb' AND table_name LIKE 'icd%';
-- 기대: 14000~14500 / 37000~38000 근사치.
-- 3 이나 2 가 나오면 ANALYZE TABLE 이 실행되지 않은 것이다.

SHOW GRANTS FOR 'app_was'@'%';                                  -- icd_* 는 SELECT 만
```

**실측 소요시간** — 5.8MB seed, 로컬 MySQL 8.0.46 기준 **약 2초**. 수 분이 걸리면 배치 크기를 확인한다.

**⚠️ 흔한 실패**

| 증상 | 원인 | 조치 |
|---|---|---|
| 적재 0건 | 필터를 `= 'Y'` 로 작성 | `<> 'N'` 으로 교정 후 재생성 |
| `Duplicate entry 'E1140' for key 'PRIMARY'` | 마스터에 37,543행 전량 INSERT | 마스터/동의어 분리 (README §6.8) |
| 한글이 `???` | seed 파일 BOM 또는 `SET NAMES` 누락 | UTF-8(BOM 없음) 재생성 |
| 실행이 수 분째 진행 | 단건 INSERT 37,543회 | 1,000행 배치로 재생성 |
| `Data too long for column 'name_en'` | `VARCHAR(255)` 미만으로 정의 | 원본 최대 223자. DDL 확인 |
| **`ERROR 1451` (재적재 시)** | `DELETE FROM icd_code` — 차트가 참조 중 | UPSERT 방식 seed 사용 (README §6.10) |
| **적재 성공했는데 검색이 느림** | `ANALYZE TABLE` 누락 | seed 말미의 `ANALYZE TABLE` 실행 확인 |
| **`ConvertFrom-Json` 실패** | PS 5.1 의 JSON 크기 한계 | `pwsh`(PS7)로 실행 |

**🔒 게이트 D2**: `icd_code` **14,283** / `icd_code_synonym` **37,543** + `app_was` 읽기 전용 확인.
WAS 담당의 `GET /staff/icd-codes` 통합 검증 **이전까지** 완료하면 된다.

---

## §6. S2 — WAS 담당

> **임계 경로. 최우선.** BFF·Web 2명이 대기 중이다.

### 6.1 코드 정합화 (D1 대기 없이 즉시 시작)

| # | 작업 | 검증 |
|---|---|---|
| 1 | `JwtTokenProvider` / `createPatientToken` / `createStaffToken` **전량 삭제** | `grep -ri "JwtTokenProvider\|createPatientToken\|createStaffToken" src/` → 0건 |
| 2 | 로그인 응답을 `{actor_id, actor_type, role, name}` 토큰 없는 형태로 교체 | 단위 테스트 |
| 3 | `spring.jackson.property-naming-strategy: SNAKE_CASE` 적용 | `patientId → patient_id` 테스트 |
| 4 | JDBC URL을 **`sslMode=REQUIRED&serverTimezone=Asia/Seoul`** 로 교체 | `grep "useSSL"` → 0건 |
| 5 | `ActorHeaderFilter` — 경로↔`actor_type`/`role` 2차 검증 (불일치 403) | 단위 테스트 |
| 6 | 인증 3개 API는 `X-Actor-*` 요구 제외 | 단위 테스트 |
| 7 | `GlobalExceptionHandler` — `{error, message, trace_id}` 통일 | 테스트 |
| 8 | 계정 잠금: 5회 → `locked_until = now+30m`, 423 | 테스트 |
| 9 | 예약 상태를 **표준 enum**으로 반환 (한글 라벨·DB 원본값 금지) | 테스트 |
| 10 | `/healthz`(프로세스) / `/readyz`(**DB 커넥션**) 구현 | curl 200 |
| 11 | 인증 API body 전체 로깅 제거 | 코드 검토 |

### 6.1b 진료·처방 도메인 — ✅ v1.1 신설

> **6.1과 병렬 진행 가능하다.** D2(ICD 적재)를 기다리지 않는다 — 로컬 MySQL에 소량 샘플만 넣고 개발한다.
> 계약은 README §6.6 / §6.8 / §6.10.

| # | 작업 | 검증 |
|---|---|---|
| 1 | 엔티티 5종 매핑 — `IcdCode`, `IcdCodeSynonym`, `Chart`, `Prescription`, `PrescriptionItem` | `gradle build` |
| 2 | `GET /internal/staff/patients?date=` — 당일 예약 목록 | 단위 테스트 |
| 3 | `GET /internal/staff/icd-codes?q=` — **동의어 테이블 조회**, `q` 2자 미만이면 `400 validation_error`, 상한 **50건** | 테스트 |
| 4 | `ChartService.create()` — 차트 + 처방 + 항목 **단일 `@Transactional`** | 부분 저장 없음 확인 |
| 5 | `icd_code` 미존재 시 `400 validation_error` | 테스트 |
| 6 | `chart.visit_no` UNIQUE 위반 → `409 duplicate_prescription` 로 변환 | **DB 제약을 잡아서 변환한다.** 사전 SELECT 체크만으로는 동시성에 진다 |
| 7 | `icd_name_snapshot` — 발급 시점 `icd_code.name_kr` 복사 저장 | 테스트 |
| 8 | `patient_id` 를 **`visit_no` → `appointment` 조인으로 결정**. 요청 본문 값 무시 | 위조 테스트 |
| 9 | `role != DOCTOR` 의 차트 생성 → **403** (WAS 2차 방어선) | NURSE 토큰 테스트 |
| 10 | `GET /internal/patient/prescriptions` — `X-Actor-Id` 기준 **본인 것만** | 타 환자 조회 404 |
| 11 | `GET /internal/staff/patients/{visit_no}/chart` — 차트+처방+항목 동시 반환, 미작성 시 404 | 테스트 |
| 12 | ICD 마스터에 **INSERT/UPDATE/DELETE 코드 없음** | `grep` 로 확인. GRANT가 이미 막지만 코드에서도 없어야 한다 |
| 13 | 외부 HTTP 클라이언트로 공공데이터포털 호출하는 코드 **없음** | `grep -ri "data.go.kr\|apis.data" src/` → **0건** |

> ⚠️ **6번이 핵심이다.** "먼저 SELECT 해서 있으면 409" 는 동시 요청 2건에서 둘 다 통과한다. `DataIntegrityViolationException` 을 잡아 409로 변환하는 것이 유일하게 옳은 구현이다. 예약의 `slot_taken` 과 동일 원칙(README §6.8).
> ⚠️ **13번** — README §16-19 금지 항목이다. 요구사항 문장("WAS가 API를 받아와 저장한다")을 그대로 구현하면 위반이다. 적재는 DB seed가 담당한다.

### 6.2 로컬 통합 검증

> ✅ **v1.1 — `local/` 일체로 자동화되었다.** 아래 4줄이 전부다.
>
> ```powershell
> cd local
> .\prepare.ps1                          # digest·시크릿·mysql-init 생성
> docker compose up --build -d --wait     # --wait 필수. 없으면 기동 전에 반환한다
> .\verify-was.ps1                        # WAS 계약 검증 38개
> .\verify-bff.ps1                        # BFF 계약 검증 45개
> ```
>
> **게이트 D3** `/readyz` → `{"status":"ok","db":"up"}` (= `ddl-auto: validate` 통과)
> **게이트 W2** `PASS 36 / FAIL 0`
>
> DDL 을 고쳤다면 `docker compose down -v` → `prepare.ps1` 재실행이 필수다.
> init 스크립트는 빈 볼륨에서만 돈다.


```powershell
cd local
docker compose up -d mysql was
docker compose logs -f was
```

| # | 검증 항목 | 기대 |
|---|---|---|
| 1 | `/healthz` | 200 |
| 2 | `/readyz` | 200 (DB 연결됨) |
| 3 | `POST /internal/patient/auth/register` | 201 + `{actor_id, role, patient}` |
| 4 | 중복 `login_id` 재가입 | 409 `patient_exists` |
| 5 | `POST /internal/patient/auth/login` 정상 | 200 |
| 6 | 비밀번호 오류 | 401 `invalid_credentials` |
| 7 | 존재하지 않는 계정 | 401 `invalid_credentials` (**404 아님**) |
| 8 | 5회 연속 실패 후 6번째 | **423 `account_locked`** |
| 9 | `POST /internal/staff/auth/login` | 200 + `role=DOCTOR` |
| 10 | `X-Actor-Type=STAFF`로 `/internal/patient/**` 호출 | **403** |
| 11 | 응답 JSON 필드가 전부 snake_case | 육안 + 테스트 |

### 6.3 ECR 푸시 및 배포

```powershell
# ① digest 핀 확인 후 빌드
cd apps/was
$TAG = (git rev-parse --short HEAD)
$REG = "597106152264.dkr.ecr.ap-northeast-2.amazonaws.com"
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin $REG
docker build --platform linux/amd64 -t "${REG}/hybrid-toy/was:${TAG}" .
docker push "${REG}/hybrid-toy/was:${TAG}"

# ② Secret / ConfigMap
kubectl -n app create secret generic was-secret --from-literal=DB_USER=app_was --from-literal=DB_PASSWORD='<DB팀 발급>'
kubectl -n app apply -f ../../k8s/was/configmap.yaml

# ③ 이미지 태그 반영 후 배포
kubectl -n app apply -f ../../k8s/was/deployment.yaml -f ../../k8s/was/service.yaml
kubectl -n app rollout status deploy/was
```

**검증**

```powershell
kubectl -n app get pod -l app=was
kubectl -n app logs deploy/was --tail=50
kubectl -n app get endpoints was-svc                        # 비어 있으면 셀렉터 불일치
kubectl -n app run tmp --rm -it --image=curlimages/curl --restart=Never -- curl -s -o /dev/null -w "%{http_code}\n" http://was-svc.app.svc.cluster.local:8080/readyz
```

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| `CrashLoopBackOff` + `Communications link failure` | JDBC TLS 옵션 누락 |
| `exec format error` | `--platform linux/amd64` 미지정 |
| `ImagePullBackOff` | 태그 오타 / 푸시 미완료 |
| `/readyz` 503 | DB 커넥션 실패. Secret 값 확인 |
| `endpoints` 비어 있음 | Service `selector` ↔ Pod `labels` 불일치 |

**🔒 게이트 W1**: `was-svc:8080/readyz` → **200**. BFF 담당에게 즉시 통보.

---

## §7. S3 — BFF 담당

> W1 통과 후 배포. **코드 작업은 W1 대기 없이 즉시 시작한다.**

### 7.1 코드 구현

| # | 작업 | 검증 |
|---|---|---|
| 1 | Cognito 의존성·코드 전량 제거 | `grep -ri cognito` → 0건 |
| 2 | `jjwt` 의존성 제거, Nimbus(`JwtEncoder`/`JwtDecoder`) 채택 | `build.gradle.kts` 확인 |
| 3 | HS256 + `JWT_SIGNING_KEY` 환경변수 주입 | 기동 확인 |
| 4 | **`TraceIdFilter`** — UUID 생성, 외부 `X-Trace-Id` 무시, 응답 헤더 부착 | 테스트 |
| 5 | **`InboundActorHeaderStripFilter`** — 외부 `X-Actor-*` 제거 (**필터 체인 최상단**) | 위조 헤더 테스트 |
| 6 | `GET /api/bff/patient/auth/csrf` + CSRF 보호 (POST/PATCH/DELETE) | 테스트 |
| 7 | `PATIENT_TOKEN` 쿠키 — HttpOnly/Secure/Lax/Path/1800s | `Set-Cookie` 헤더 확인 |
| 8 | 전달수단↔`actor_type` 바인딩 검증 | **4케이스** 테스트 (아래) |
| 9 | 경로 제한 — PATIENT는 `/patient/**`, STAFF는 `/staff/**` | 테스트 |
| 10 | `role` → `ROLE_*` 권한 변환 | 테스트 |
| 11 | `WasClient` — connect 2s / read 5s / **retry 없음** | 장애 주입 |
| 12 | WAS 5xx·timeout → **503 `upstream_unavailable`** 변환 | 장애 주입 |
| 13 | WAS 400/401/404/409/423 → 그대로 전달 | 테스트 |
| 14 | JWT 발급 시 `X-Actor-*` 재생성 후 WAS 전달 | 테스트 |
| 15 | `/healthz`, `/readyz` (**WAS에 종속시키지 않음** — README §7.9) | curl 200 |
| 16 | 직원 JWT TTL **8시간**, 환자 **30분** | 디코딩 확인 |

**8번 — 바인딩 4케이스 (전부 401이어야 한다)**

| 케이스 | 기대 |
|---|---|
| STAFF JWT를 `PATIENT_TOKEN` 쿠키로 전달 | 401 |
| PATIENT JWT를 `Authorization: Bearer`로 전달 | 401 |
| `actor_type` claim 없는 JWT | 401 |
| 알 수 없는 `actor_type` | 401 |

### 7.2 로컬 전체 통합

```powershell
cd local
docker compose up -d          # mysql + was + bff + patient-web
```

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | `GET /api/bff/patient/auth/csrf` | 200 + `XSRF-TOKEN` 쿠키 |
| 2 | CSRF 헤더 없이 register | 403 |
| 3 | CSRF 헤더 포함 register | 201 + `PATIENT_TOKEN` 쿠키 |
| 4 | 쿠키 없이 `GET /appointments` | 401 |
| 5 | 쿠키 포함 `GET /appointments` | 200 |
| 6 | 요청에 `X-Actor-Id: 999` 위조 삽입 | **무시됨** (WAS 로그에 999 없음) |
| 7 | WAS 컨테이너 정지 후 API 호출 | **503 `upstream_unavailable`** |
| 8 | WAS 재기동 후 재호출 | 200 (복구) |
| 9 | 직원 로그인 → Bearer로 `/staff/patients` | 200 |
| 10 | NURSE 토큰으로 `POST /staff/charts` | **403** |
| 11 | 오류 응답 3필드 + `X-Trace-Id` 헤더 | 전부 존재 |
| 12 | BFF 로그와 WAS 로그의 `trace_id` 일치 | 동일 UUID |

### 7.3 배포

```powershell
cd apps/bff
$TAG = (git rev-parse --short HEAD)
$REG = "597106152264.dkr.ecr.ap-northeast-2.amazonaws.com"
docker build --platform linux/amd64 -t "${REG}/hybrid-toy/bff:${TAG}" .
docker push "${REG}/hybrid-toy/bff:${TAG}"

kubectl -n app create secret generic bff-secret --from-literal=JWT_SIGNING_KEY='<리드 발급>'
kubectl -n app apply -f ../../k8s/bff/configmap.yaml
kubectl -n app apply -f ../../k8s/bff/deployment.yaml -f ../../k8s/bff/service.yaml
kubectl -n app rollout status deploy/bff
```

**검증**

```powershell
kubectl -n app run tmp --rm -it --image=curlimages/curl --restart=Never -- sh -c "curl -s -o /dev/null -w '%{http_code}\n' http://bff-svc.app.svc.cluster.local:8080/healthz; curl -s -i http://bff-svc.app.svc.cluster.local:8080/api/bff/patient/auth/csrf | head -20"
```

**🔒 게이트 B1**: 클러스터 내부에서 `csrf → register → login` 성공 + `PATIENT_TOKEN` 발급 확인. Web 담당에게 통보.

---

## §8. S4 — Web 담당

> B1 통과 후. **이미지 빌드는 그 전에 끝내 둔다.**

| # | 작업 | 검증 |
|---|---|---|
| 1 | `nginx.conf` — `listen 8080`, non-root | 로컬 curl |
| 2 | `/healthz`, `/readyz` 정적 200 | 로컬 curl |
| 3 | SPA fallback (`try_files ... /index.html`) | 새로고침 시 404 안 남 |
| 4 | **`location /api/bff/staff { return 404; }`** 명시 | curl 404 |
| 5 | API 호출은 상대경로 `/api/bff/patient` (도메인 하드코딩 없음) | `grep -r "https://"` 확인 |
| 6 | `credentials: "include"` 적용 | 코드 검토 |
| 7 | 이미지 빌드·푸시 | ECR 확인 |
| 8 | Deployment + Service 배포 | Pod Running |
| 9 | **Ingress apply** | 아래 |
| 10 | ALB DNS명 확보 → 리드 전달 | — |

**9번 — Ingress (README §11.3 확정본 사용)**

```powershell
# ACM ARN을 손으로 복사하지 않는다
$ACM = "arn:aws:acm:ap-northeast-2:597106152264:certificate/6eecdf13-ea45-4847-964b-ae9642308f87"
(Get-Content ..\..\k8s\ingress\patient-web-ingress.yaml) -replace '<ACM_ARN>', $ACM | kubectl -n app apply -f -

# 🚨 적용 직후 반드시 대조 — ARN 절단 3회차 방지
kubectl -n app get ingress patient-web -o jsonpath="{.metadata.annotations}"
```

```powershell
kubectl -n app get ingress patient-web -w      # ADDRESS 표시까지 ~3분
```

**Target Group 상태 확인**

```powershell
kubectl -n kube-system logs deploy/aws-load-balancer-controller --tail=50
kubectl -n app describe ingress patient-web
```

**⚠️ 흔한 실패**

| 증상 | 원인 |
|---|---|
| ADDRESS가 영원히 빈칸 | LBC 로그 확인. IRSA 권한 / 서브넷 태그 |
| Target Group unhealthy | `/healthz` 미구현 또는 경로 불일치 |
| API 요청이 Web으로 감 | Ingress에서 `/`를 먼저 선언함 → **`/`를 마지막으로** |
| ALB가 인증서 오류 | ACM ARN 절단 |

**🔒 게이트 W2**: ADDRESS 표시 + 두 Target Group(`bff-svc`, `patient-web-svc`) 모두 **healthy**.

---

## §9. S5 — EKS 배포 → 🚩 G2  ✅ v1.2 재작성

### 9.1 사전 준비 (1회)

| # | 작업 | 담당 |
|---|---|---|
| 1 | `aws eks update-kubeconfig --name hybrid-toy-eks --region ap-northeast-2` | 각자 |
| 2 | `kubectl apply --server-side -f k8s/namespace/` | EKS팀 |
| 3 | `was-secret` 생성 (`DB_USER=app_was`, `DB_PASSWORD`) | EKS팀 |
| 4 | `bff-secret` 생성 (Secrets Manager 에서 `JWT_SIGNING_KEY` 인출) | EKS팀 |
| 5 | `apps/patient-web/Dockerfile` digest 치환 | EKS팀 |
| 6 | `k8s/ingress/` ACM ARN 치환 | EKS팀 |

**3~4번 — 값을 명령행에 남기지 않는다**

```powershell
$pw = Read-Host "app_was 비밀번호" -AsSecureString
$pt = [Runtime.InteropServices.Marshal]::PtrToStringBSTR(
        [Runtime.InteropServices.Marshal]::SecureStringToBSTR($pw))
kubectl -n app create secret generic was-secret `
  --from-literal=DB_USER=app_was --from-literal=DB_PASSWORD=$pt
$pt = $null

$k = (aws secretsmanager get-secret-value --secret-id bff-secret --region ap-northeast-2 `
       --query SecretString --output text | ConvertFrom-Json).JWT_SIGNING_KEY
kubectl -n app create secret generic bff-secret --from-literal=JWT_SIGNING_KEY=$k
$k = $null
```

**6번 — ACM ARN 은 CLI 로 받는다.** GUI 나 마크다운에서 복사하면 값이
조용히 절단된다 (README §15, 3회 사고).

```powershell
aws acm list-certificates --region ap-northeast-2 `
  --query "CertificateSummaryList[].{Domain:DomainName,Arn:CertificateArn}" --output table
```

치환은 §3.5 의 `Set-Placeholder` 로만 한다.

### 9.2 사전 점검

```powershell
cd scripts
.\preflight.ps1
```

14개 항목을 본다. **FAIL 이 남은 채 배포하지 않는다.** 파드가
CrashLoopBackOff 로 돌기 시작하면 원인이 코드인지 설정인지 구분되지 않는다.

> `cloudflared digest 치환됨` FAIL 은 G3 전까지 정상이다.

### 9.3 배포 — 의존 역순

```powershell
.\deploy.ps1 -Service was          -Action all
.\deploy.ps1 -Service bff          -Action all
.\deploy.ps1 -Service patient-web  -Action all
```

⚠️ **커밋하지 않으면 스크립트가 중단한다.** 태그가 git short SHA 이므로
미커밋 상태로 빌드하면 이미지와 커밋이 어긋나 추적이 불가능해진다.

⚠️ **ECR 은 immutable tag 다.** 같은 태그를 두 번 푸시할 수 없다.
배포할 때마다 README §22.5 표에 태그를 기록한다.

### 9.4 Ingress 적용 — ALB 실제 생성

```powershell
kubectl apply --server-side --force-conflicts -f ..\k8s\ingress\
kubectl -n app get ingress patient-web -w        # ADDRESS 붙기까지 2~4분
```

**적용 직후 ARN 대조 — 필수**

```powershell
kubectl -n app get ingress patient-web `
  -o jsonpath="{.metadata.annotations.alb\.ingress\.kubernetes\.io/certificate-arn}"
```

`certificate/` 뒤 36자 UUID 가 온전해야 한다.

**ALB DNS 확보 → 리드 전달**

```powershell
kubectl -n app get ingress patient-web -o jsonpath="{.status.loadBalancer.ingress[0].hostname}"
```

### 9.5 리드 작업 (Cloudflare)

| # | 작업 | 검증 |
|---|---|---|
| 1 | SSL/TLS = **Full (strict)** | 대시보드 |
| 2 | `www` CNAME → ALB DNS, **🟠 주황 구름 ON** | 대시보드 |
| 3 | DNS 전파 | `nslookup www.kuspitalsoldeskproject.org` |
| 4 | WAF Managed Ruleset / Rate Limiting / Bot Fight Mode | 대시보드 |

### 9.6 🔒 게이트 G2

**① 스크립트**

```powershell
.\verify-flow1.ps1        # PASS 24 / FAIL 0
```

**② 브라우저 — 이것이 최종 조건이다**

- [ ] `https://www.kuspitalsoldeskproject.org` 화면 표시
- [ ] **가입 → 403 이 나지 않음**
- [ ] 로그인 상태 표시 (우상단 이름)
- [ ] 의사·날짜 선택 → 시간 슬롯 표시
- [ ] 예약 → 접수번호 `V20260812-XXXX`
- [ ] 내 예약에 카드 표시
- [ ] **새로고침 후 로그인 유지**
- [ ] 로그아웃 → 새로고침 시 가입 화면
- [ ] 콘솔에 403·CORS 오류 없음 (초기 401 1회는 정상 — README §22.7)
- [ ] Cloudflare Analytics 에 트래픽 기록

> 🚨 **스크립트만으로 G2 를 닫지 않는다.**
> `verify-bff` 45개 + `verify-flow1` 22개를 통과한 상태에서 브라우저
> 회원가입이 403 으로 실패한 이력이 있다 (README §22.2).
> 하네스는 브라우저보다 관대하다.

---

## §10. S6 — 내부 흐름 → 🚩 G3

### 10.1 리드 (Cloudflare)

| # | 작업 | 산출물 → 전달 |
|---|---|---|
| 1 | Tunnel 생성 | Tunnel Token → BFF |
| 2 | Public Hostname: `staff-api.kuspitalsoldeskproject.org` → `http://bff-svc.app.svc.cluster.local:8080` | — |
| 3 | Access Application 생성 (위 도메인) | — |
| 4 | Policy **Action = `Service Auth`** | — |
| 5 | Service Token 발급 | ID + Secret → OKD |
| 6 | Tunnel에 **Protect with Access 활성화 + 올바른 AUD** | — |

### 10.2 BFF 담당 (cloudflared)

```powershell
kubectl -n app create secret generic cloudflared-secret --from-literal=TUNNEL_TOKEN='<리드 발급>'
kubectl -n app apply -f k8s/cloudflared/deployment.yaml
kubectl -n app rollout status deploy/cloudflared
kubectl -n app logs deploy/cloudflared --tail=30
```

**검증**: 로그에 터널 등록 성공 + Cloudflare 대시보드 Tunnel **HEALTHY** (replica 2 모두).

### 10.3 OKD팀

| # | 작업 |
|---|---|
| 1 | `cf-access-token` Secret 생성 (README §18.6) |
| 2 | Web Pod 환경변수 주입 (`BFF_BASE_URL` + Access 2종) |
| 3 | 서버사이드 프록시 구현 — Access 헤더 부착 |
| 4 | 직원 로그인 → JWT를 **서버사이드 세션**에 보관 |
| 5 | 업무 API 호출 시 `Authorization: Bearer` 추가 |
| 6 | 표준 enum → 직원 화면 한글 라벨 변환 |
| 7 | 로그아웃 시 세션·JWT 삭제 |

**단계별 진단** (이 순서로 좁힌다)

```bash
# ① Access 통과 여부만 확인 — 401 JSON이면 통과한 것
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" \
  https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/patients \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>"

# ② 직원 로그인
curl -s -X POST https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/auth/login \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>" \
  -H "Content-Type: application/json" \
  -d '{"login_id":"doc_kim","password":"<seed 비밀번호>"}'

# ③ Bearer로 업무 API
curl -s https://staff-api.kuspitalsoldeskproject.org/api/bff/staff/patients \
  -H "CF-Access-Client-Id: <ID>" -H "CF-Access-Client-Secret: <SECRET>" \
  -H "Authorization: Bearer <STAFF_JWT>"
```

| ①의 결과 | 판정 |
|---|---|
| `200` / `401` + `application/json` | ✅ Access 통과. BFF까지 도달 |
| `302` 또는 `200 text/html` | ❌ Policy Action이 `Allow` (리드 조치) |
| `403 application/json` | ❌ Service Token 값 오류 (OKD 조치) |
| 연결 실패 | ❌ 아웃바운드 443 / DNS / 프록시 (OKD 조치) |
| `502` / `504` | ❌ cloudflared Pod 또는 `bff-svc` 문제 (BFF 조치) |

**🔒 게이트 G3 — ✅ 2026-08-13 통과 (`verify-flow2.ps1` 29/29)**

- [x] Access 통과 → BFF 도달 (`401 application/json`)
- [x] 직원 로그인 → Bearer JWT (exp = 8시간)
- [x] DOCTOR가 차트 작성 성공 / NURSE가 차트 작성 **403**
- [x] ADMIN_STAFF만 `/staff/admin/**` 접근 (경로 미구현이라 `404`, `403` 아님)
- [x] 잘못된 Service Token → **403**
- [x] 위조 서명 JWT → **401**
- [x] 위조 `X-Actor-*` 헤더 주입 → **401**
- [x] `X-Trace-Id`가 응답 헤더에 존재

> **실행 중 발견** — 검증 스크립트가 최초 2건을 오탐지했다. Cloudflare Access 의
> 차단 응답 자체가 `text/html`(자체 오류 페이지)인데, 이를 "Policy가 Allow일 때의
> 로그인 폼 신호"와 혼동해 판정 조건에 `Content-Type` 을 걸어둔 것이 원인이다.
> 인프라는 처음부터 정상이었다. 판정을 상태코드로만 좁혀 교정했다 (README §22.7).

---

## §11. 통합 검증 시나리오

> ✅ **v1.2 — 현재 시나리오 건수**
>
> | 스크립트 | 대상 | 건수 | 게이트 |
> |---|---|---|---|
> | `local/verify-was.ps1` | WAS 직접 (18080) | **38** | W2 |
> | `local/verify-bff.ps1` | BFF 경유 (18081) | **45** | B2 |
> | `scripts/verify-flow1.ps1` | 도메인 경유 (HTTPS) | **24** | G2 |
> | `scripts/verify-flow2.ps1` | Tunnel 경유 | ⬜ | G3 |
>
> 로컬 compose 뿐 아니라 **EKS 에도 그대로 쓴다.**
> `kubectl -n app port-forward svc/bff-svc 18081:8080` 후 `verify-bff.ps1` 을 실행하면
> 포트가 같아 인자 없이 동작한다.

### 11.1 흐름1 — 외부 환자 (G2)

```powershell
$BASE = "https://www.kuspitalsoldeskproject.org"
$s = New-Object Microsoft.PowerShell.Commands.WebRequestSession

# 1. 화면
(Invoke-WebRequest "$BASE/" -WebSession $s).StatusCode                       # 200

# 2. CSRF
$csrf = Invoke-RestMethod "$BASE/api/bff/patient/auth/csrf" -WebSession $s
$hdr  = @{ "X-XSRF-TOKEN" = $csrf.token }

# 3. 회원가입
$body = @{ login_id="testpt01"; password="P@ssw0rd123"; name="테스트"; birth_date="1990-03-14" } | ConvertTo-Json
Invoke-RestMethod "$BASE/api/bff/patient/auth/register" -Method Post -Body $body -ContentType "application/json" -Headers $hdr -WebSession $s

# 4. 인증 후 조회
Invoke-RestMethod "$BASE/api/bff/patient/appointments" -WebSession $s

# 5. 직원 API 차단 확인 — 404 여야 정상
try { Invoke-WebRequest "$BASE/api/bff/staff/auth/login" -Method Post } catch { $_.Exception.Response.StatusCode.value__ }
```

| # | 항목 | 기대 |
|---|---|---|
| 1 | 화면 로드 | 200 |
| 2 | CSRF | 200 + `XSRF-TOKEN` 쿠키 |
| 3 | 회원가입 | 201 + `PATIENT_TOKEN` 쿠키 |
| 4 | 인증 API | 200 |
| 5 | 직원 API | **404** |
| 6 | 5회 오입력 후 | **423** |
| 7 | 타 환자 데이터 접근 | **404** |
| 8 | 로그아웃 후 재호출 | **401** |

### 11.2 흐름2 — 내부 직원 (G3)

§10.3의 3단계 curl을 순서대로 수행. 각 단계의 실패 판정표를 그대로 사용한다.

### 11.3 진료·처방 시나리오 — ✅ v1.1 신설

> 흐름2(터널) 경유로 수행한다. G3 조건에 포함된다.

```powershell
$BASE = "https://staff-api.kuspitalsoldeskproject.org"
$CF   = @{ "CF-Access-Client-Id" = "<ID>"; "CF-Access-Client-Secret" = "<SECRET>" }

# 1. 의사 로그인
$doc = Invoke-RestMethod "$BASE/api/bff/staff/auth/login" -Method Post -Headers $CF `
  -ContentType "application/json" -Body '{"login_id":"doc_kim","password":"<seed>"}'
$H = $CF + @{ "Authorization" = "Bearer $($doc.access_token)" }

# 2. 당일 예약 목록
$list = Invoke-RestMethod "$BASE/api/bff/staff/patients" -Headers $H
$visit = $list[0].visit_no

# 3. 상병코드 검색
Invoke-RestMethod "$BASE/api/bff/staff/icd-codes?q=당뇨" -Headers $H | Measure-Object

# 4. 차트 + 처방 발급
$body = @{
  visit_no = $visit
  icd_code = "E1140"
  chief_complaint = "다뇨, 하지 저림"
  note = "혈당 조절 불량. 2주 후 재내원."
  prescription_items = @(
    @{ drug_name = "메트포르민정 500mg"; dosage = "1정"; frequency = "1일 2회"; duration_days = 14 }
  )
} | ConvertTo-Json -Depth 5
Invoke-RestMethod "$BASE/api/bff/staff/charts" -Method Post -Headers $H -ContentType "application/json" -Body $body

# 5. 차트 재조회
Invoke-RestMethod "$BASE/api/bff/staff/patients/$visit/chart" -Headers $H
```

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | 당일 예약 목록 조회 | 200 + `visit_no` 포함 |
| 2 | `?q=당뇨` 검색 | 200 + **50건 이하** |
| 3 | `?q=E` (1자) | **400 `validation_error`** |
| 4 | DOCTOR 차트+처방 발급 | **201** |
| 5 | 동일 `visit_no` 재발급 | **409 `duplicate_prescription`** |
| 6 | NURSE 토큰으로 발급 | **403** |
| 7 | 존재하지 않는 `icd_code` | **400 `validation_error`** |
| 8 | 본문에 `patient_id` 위조 삽입 | **무시됨** — DB에는 예약의 실제 환자 |
| 9 | 차트 재조회 | 200 + 처방 항목 1건 |
| 10 | 환자 로그인 → `GET /api/bff/patient/prescriptions` | 200 + **본인 것만** |
| 11 | 다른 환자의 `visit_no` 조회 | **404** |

> 8번은 위조 방어 회귀 테스트다. §6.1b-8번이 깨지면 여기서 잡힌다.

### 11.4 관측성 검증 — 두 흐름 공통

```powershell
# 오류를 하나 유발한 뒤 trace_id로 양쪽 로그를 대조한다
kubectl -n app logs deploy/bff --tail=200 | Select-String "<trace_id>"
kubectl -n app logs deploy/was --tail=200 | Select-String "<trace_id>"
```

- [ ] 오류 응답 JSON에 `error` / `message` / `trace_id` 3필드
- [ ] 응답 헤더에 `X-Trace-Id`
- [ ] BFF·WAS 로그에서 동일 `trace_id`
- [ ] **성공 응답에는 `trace_id` 없음**
- [ ] 로그에 비밀번호·JWT·토큰 문자열 **없음**

---

## §12. 장애 시 롤백

### 12.1 배포 롤백

```powershell
kubectl -n app rollout history deploy/<서비스>
kubectl -n app rollout undo deploy/<서비스>
kubectl -n app rollout status deploy/<서비스>
```

> 이미지가 `IMMUTABLE` + git SHA 태그이므로 **어느 커밋인지 즉시 역추적된다.** 이것이 §7.5의 실효다.

### 12.2 절대 하지 말 것

| 금지 | 결과 |
|---|---|
| `kubectl delete -f k8s/` | Namespace 포함 시 **하위 전체 캐스케이드 삭제** |
| `terraform destroy` (팀원) | 단일 state 손상. 리드 전용 |
| RDS SG에 개인 IP 추가 | 격리 설계 무력화 |
| DB 스키마를 `app_was`로 변경 | 권한이 없다. DB팀이 `admin`으로 수행 |
| Ingress에 `/api/bff/staff` 추가 | 직원 API 외부 노출 |

### 12.3 DB 롤백

`skip_final_snapshot = true` 이므로 **RDS destroy = 데이터 완전 소실**이다. 복구 경로는 git의 DDL·seed 재실행뿐이다. 그래서 §5-4번(커밋)이 필수 단계다.

---

## §13. 코드 산출 이력  ✅ v1.2 갱신

한 번에 전부 쏟아내면 검증 불가능한 덩어리가 된다. **배치로 끊어서 갔다.**

| 배치 | 산출물 | 검증 게이트 | 상태 |
|---|---|---|---|
| **1** | `db/01~03.sql` + `tools/gen-bcrypt.ps1` | 로컬 MySQL 8.0.46 실행 | ✅ |
| **1.5** | `tools/convert-icd.ps1` → `db/04_icd_seed.sql` | 14,283 / 37,543 | ✅ |
| **2a** | WAS 빌드·설정·엔티티 9·리포지토리 8 | 컴파일 | ✅ |
| **2b** | WAS 필터·오류·인증·업무 + `JpaBootstrapTest` | **W1** | ✅ |
| **2c** | `local/` compose + `verify-was.ps1` | **D3 · W2 (38/38)** | ✅ |
| **3a** | BFF `JwtIssuer`·`TokenResolver`·`SecurityConfig` | JWT 계약 5 | ✅ |
| **3b** | BFF 컨트롤러·`GlobalExceptionHandler` | 바인딩 계약 7 | ✅ |
| **3c** | compose 3단 + `verify-bff.ps1` | **B1 · B2 (45/45)** | ✅ |
| **4a** | `k8s/` 매니페스트 + `deploy.ps1` + `preflight.ps1` | 파드 기동 | ✅ |
| **4b** | `apps/patient-web/` + Ingress + `verify-flow1.ps1` | **G2 (24/24 + 브라우저)** | ✅ |
| **5** | cloudflared 배포 + `verify-flow2.ps1` | **G3** | ⬜ |

각 배치는 다음 형식으로 산출한다.

1. 파일 트리
2. 파일 전문
3. 로컬 검증 명령
4. 통과 기준
5. 다음 배치 진입 조건

### 13.1 배치 진행에서 확인된 것

**① 검증 스크립트를 배치마다 함께 낸다.** 코드만 받고 검증을 나중에 만들면
"동작하는 것처럼 보이는 상태"가 누적된다. `JpaBootstrapTest` 가 없었다면
`@Query` JPQL 15개는 EKS 첫 기동에서야 검증됐을 것이다.

**② 실패는 배치 안에서 닫는다.** 다음 배치로 넘기면 원인이 섞인다.
BFF 는 컴파일 단계에서만 4번 막혔고 (Boot 4 이관), 전부 그 자리에서 닫았다.

**③ 검증 통과가 동작을 보증하지 않는다.** G2 에서 실증됐다 (README §22.2).
하네스의 관대함을 계약으로 착각하지 않는다.

---

## §14. 배포 전 체크리스트  ✅ v1.2 신설

배포 직전에 이 목록을 한 번 훑는다. 전부 실패 이력이 있는 항목이다.

### 14.1 코드

- [ ] `.\gradlew clean build` 통과 (WAS·BFF 각각)
- [ ] 주석에 `**/` 없음 — Javadoc 조기 종료
  ```powershell
  Select-String -Path apps -Include *.java -Pattern '\*\*/' -Recurse
  ```
- [ ] `application.yaml` 의 값이 ConfigMap 계약과 일치 (특히 `JWT_AUDIENCE`)

### 14.2 매니페스트

- [ ] 자리표시자 잔존 0건
  ```powershell
  Select-String -Path k8s -Include *.yaml -Pattern '__[A-Z_]+__' -Recurse
  ```
- [ ] **파드 limits × replicas ≤ ResourceQuota**
  ```powershell
  kubectl -n app describe resourcequota app-quota
  ```
- [ ] 이미지 태그가 `:latest` 아님
- [ ] Ingress 경로 순서 — `/api/bff/patient` 먼저, `/` 마지막
- [ ] Ingress 에 `/api/bff/staff` 규칙 **없음**

### 14.3 시크릿·파일

- [ ] `.\preflight.ps1` 전 항목 통과
- [ ] 치환한 파일에 BOM 없음
- [ ] `git status` 에 `.env` / `mysql-init/` / `*.local.sql` 없음
- [ ] 커밋 완료 (미커밋이면 `deploy.ps1` 이 중단한다)

### 14.4 배포 후

- [ ] `kubectl -n app get pod` 전부 `Running` `1/1`
- [ ] 파드가 두 노드에 분산
- [ ] 배포 태그를 README §22.5 에 기록
- [ ] 검증 스크립트 실행

---

## 프로젝트 완료 — ✅ 2026-08-13

G1 → G3 뼈대 완성. OKD Web 구현은 프로젝트 기간 제약으로 범위 밖 이관.
재개 시 README §17.3(백로그)·§22(실행 기록)를 시작점으로 삼는다.
계약(README §6)은 이 시점 이후 변경되지 않았으므로 그대로 유효하다.
